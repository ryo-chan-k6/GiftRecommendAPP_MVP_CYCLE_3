{
  "name": "gift-recommendation-service",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "orval": "orval",
    "dev:web": "pnpm --filter @gift-recommendation/web dev",
    "dev:api": "pnpm --filter @gift-recommendation/api dev",
    "dev:reco": "pnpm --filter @gift-recommendation/reco dev",
    "dev:batch": "pnpm --filter @gift-recommendation/batch dev"
  },
  "devDependencies": {
    "orval": "^8.15.0",
    "typescript": "^5.8.3"
  }
}
