packages:
  - "apps/*"
  - "packages/*"

# pnpm 11: dependency build scripts must be explicitly allowed (Render / CI).
# Without this, install fails with ERR_PNPM_IGNORED_BUILDS (esbuild / sharp / unrs-resolver).
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
