# テーブル一覧

## 1. 作成方針

| 観点         | 方針                                                                                                       |
| ------------ | ---------------------------------------------------------------------------------------------------------- |
| 目的         | 物理ER / テーブル定義 / DDL作成の前段として、永続化対象テーブルを一覧化する                                |
| 対象         | MVPで利用するOnline推薦、Batch商品連携、Feature / Semantic、評価、ログ・メトリクス系テーブル               |
| 除外         | メモリ上の一時データ、Object Storage上のRaw JSON本体、APIレスポンスそのもの                                |
| テーブル粒度 | 正本・派生・Snapshot・Log・Metricを混在させず、用途ごとに分離する                                          |
| 状態管理     | 業務状態を持つテーブルは状態カラムを保持する。詳細な遷移履歴は `phase_log` / `error_log` に記録する        |
| Ranking方針  | ランキングはマスタではなくSnapshotとして管理し、`ranking_snapshot` と `item_popularity_signal` に分離する  |
| Feature方針  | User FeatureはOnlineで生成、Item FeatureはBatchで事前生成する。ただし同一Feature Engineの利用を前提とする  |
| Version方針  | Feature生成・正規化・Semantic変換は `semantic_config_version` / `feature_normalization_version` と紐づける |
| 入力hash方針 | Item Feature / Item Embeddingは、元商品データ更新により再生成が必要になるため、入力hashを保持する          |

### 1.1 テーブル仕様書作成時の識別子方針

| 観点 | 方針 |
| ---- | ---- |
| Task識別子 | `TABLE-XXX` は新設せず、物理テーブル名を使用する |
| Task / Issueタイトル | `{物理テーブル名}:{概要}` を基本とする |
| 成果物ファイル名 | `{物理テーブル名}_テーブル定義書.md` を基本とする |
| 正本一覧 | 本ドキュメントの `テーブル名` 列を正本とする |

---

## 2. 全体テーブル分類

| 分類                  | 主なテーブル                                                                                                                                                   | 位置づけ                                                                                             |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Online推薦系          | `recommendation_request` / `recommendation_run` / `recommendation_result` / `recommendation_result_item` / `recommendation_reason` / `recommendation_feedback` | ユーザー入力、推薦実行、推薦結果、理由、Feedback                                                     |
| User意味推定系        | `user_semantic` / `user_feature` / `user_meaning`                                                                                                              | Online推薦時に生成されるユーザー側派生データ                                                         |
| Item系                | `item` / `item_image` / `item_review_summary` / `item_popularity_signal`                                                                                       | Online推薦で参照する商品正本・補助情報                                                               |
| Ranking Snapshot系    | `ranking_snapshot` / `item_popularity_signal`                                                                                                                  | 楽天ランキングの取得単位とランキング明細                                                             |
| 外部商品データ連携系  | `fetch_cursor` / `raw_product_metadata` / `staging_item` / `product_diff_result` 等                                                                            | BatchによるRaw保存、Staging変換、Item反映                                                            |
| Semantic / Feature系  | `semantic_config` / `semantic_config_version` / `semantic_concept` / `feature_definition` / `semantic_rule` / 各種Feature Rule / `normalization_rule`            | 意味定義、Feature定義、変換ルール、正規化 binding                                                    |
| Item派生データ系      | `item_semantic` / `item_feature` / `item_meaning` / `item_embedding` / `item_generation_queue`                                                                 | Batchで事前生成する推薦用派生データ                                                                  |
| Master / Config系     | `relationship_master` / `occasion_master` / `pair_master` / `model_version` / `ranking_config` / `reason_template` / `feature_normalization_version`           | 入力マスタ、Relationship × Occasion組み合わせ、参照設定、モデル・ランキング・理由生成・正規化version |
| Evaluation系          | `evaluation_dataset` / `evaluation_case` / `evaluation_run` / `evaluation_result` / `evaluation_metric`                                                        | オフライン評価・検証                                                                                 |
| Log / Observability系 | `batch_run_log` / `phase_log` / `error_log` / `api_call_log` / 各種metric                                                                                      | 実行記録、エラー、外部API呼び出し、分布監視                                                          |

---

## 3. Online推薦系テーブル一覧

| No  | テーブル名                   | 論理名                     | 正本区分            | MVP | 主な更新主体 | 主な用途                             |
| --- | ---------------------------- | -------------------------- | ------------------- | --- | ------------ | ------------------------------------ |
| 1   | `recommendation_request`     | Recommendation Request     | 内部正本            | ○   | api / reco   | ユーザーの推薦依頼条件を保存する     |
| 2   | `recommendation_run`         | Recommendation Run         | 実行正本 / 状態     | ○   | reco         | 推薦処理1回分の実行単位を保存する    |
| 3   | `recommendation_result`      | Recommendation Result      | 内部正本            | ○   | reco         | 推薦結果全体を保存する               |
| 4   | `recommendation_result_item` | Recommendation Result Item | Snapshot / 内部正本 | ○   | reco         | 推薦結果に含まれる商品明細を保存する |
| 5   | `recommendation_reason`      | Recommendation Reason      | 派生Snapshot        | ○   | reco         | 推薦理由文・理由要素を保存する       |
| 6   | `recommendation_feedback`    | Recommendation Feedback    | 内部正本            | ○   | api          | ユーザーの評価・Feedbackを保存する   |

### 補足

| 論点                               | 方針                                                                                                                                                                                                                                               |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `recommendation_request` の `mode` | `ui` / `evaluation` / `batch` を保持する                                                                                                                                                                                                           |
| budget条件                         | `recommendation_request` に `budget_min` / `budget_max` として保持する                                                                                                                                                                             |
| request conditionの扱い            | MVPでは独立テーブル化せず、`recommendation_request` 内のカラムまたはJSONで保持する                                                                                                                                                                 |
| result item snapshot               | 商品名、価格、URL、画像URLなどは推薦結果表示時点で固定する                                                                                                                                                                                         |
| 0件結果                            | errorではなく、`recommendation_result` の正常なempty resultとして扱う                                                                                                                                                                              |
| Relationship / Occasion / Pair     | ユーザー入力としては `relationship_id` と `occasion_id` を受け取り、処理時に `pair_master` から `pair_id` を解決する。推薦再現性を重視するため、`recommendation_request` または `recommendation_run` に解決済み `pair_id` を保持することを検討する |

---

## 4. User意味推定系テーブル一覧

| No  | テーブル名      | 論理名        | 正本区分 | MVP | 主な更新主体 | 主な用途                                                |
| --- | --------------- | ------------- | -------- | --- | ------------ | ------------------------------------------------------- |
| 7   | `user_semantic` | User Semantic | 派生     | ○   | reco         | ユーザー入力から抽出したSemantic Conceptを保存する      |
| 8   | `user_feature`  | User Feature  | 派生     | ○   | reco         | ユーザー意図をFeature値として保存する                   |
| 9   | `user_meaning`  | User Meaning  | 派生     | ○   | reco         | User FeatureをSocial / Symbolicへ射影した結果を保存する |

### 補足

| 論点           | 方針                                                                         |
| -------------- | ---------------------------------------------------------------------------- |
| 実行環境       | User側はOnline推薦時に `reco` で生成する                                     |
| Feature Engine | Item側と同じFeature推定ロジックを利用する                                    |
| 保存単位       | `recommendation_run_id` 単位で保存する                                       |
| version        | `semantic_config_version_id` / `feature_normalization_version_id` を紐づける |
| 再現性         | 推薦結果の再現性確保のため、生成時点のversionを保持する                      |

---

## 5. Item系テーブル一覧

| No  | テーブル名               | 論理名                 | 正本区分            | MVP | 主な更新主体 | 主な用途                                     |
| --- | ------------------------ | ---------------------- | ------------------- | --- | ------------ | -------------------------------------------- |
| 10  | `item`                   | Item                   | 内部商品正本        | ○   | batch        | Online推薦で参照する内部商品データ           |
| 11  | `item_image`             | Item Image             | 内部正本 / 外部参照 | ○   | batch        | 商品画像URLを保存する                        |
| 12  | `item_review_summary`    | Item Review Summary    | 派生 / 補助情報     | ○   | batch        | レビュー平均・レビュー件数等を保存する       |
| 13  | `external_genre`         | External Genre         | 外部参照 / 内部正本 | ○   | batch        | 楽天ジャンル階層を保存する                   |
| 14  | `external_attribute`     | External Attribute     | 外部参照            | △   | batch        | 楽天属性情報を保存する                       |
| 15  | `ranking_snapshot`       | Ranking Snapshot       | Snapshot            | ○   | batch        | 楽天ランキング取得単位を保存する             |
| 16  | `item_popularity_signal` | Item Popularity Signal | Snapshot / 派生     | ○   | batch        | ランキング順位など人気補助シグナルを保存する |

### 補足

| 論点                                | 方針                                                                       |
| ----------------------------------- | -------------------------------------------------------------------------- |
| 商品                                | 差分反映対象。`normalized_hash` により更新判定する                         |
| ジャンル                            | 差分反映対象。内部の外部参照マスタとして保持する                           |
| 属性                                | MVPでは任意。保持する場合はFeature推定補助の候補情報として扱う             |
| ランキング                          | 差分反映ではなく、取得回ごとのSnapshotとして全件反映する                   |
| Review情報                          | MVPではFeature推定要素に含めない。popularity補助・表示・分析用途に限定する |
| `item_popularity_signal` の冪等キー | `ranking_snapshot_id + rank` を基本とする                                  |

---

## 6. 外部商品データ連携系テーブル一覧

| No  | テーブル名               | 論理名                 | 正本区分        | MVP | 主な更新主体 | 主な用途                                                      |
| --- | ------------------------ | ---------------------- | --------------- | --- | ------------ | ------------------------------------------------------------- |
| 17  | `fetch_cursor`           | Fetch Cursor           | 状態 / 管理情報 | ○   | batch        | 楽天APIの疑似差分取得カーソルを管理する                       |
| 18  | `api_call_log`           | API Call Log           | Log             | ○   | batch        | 外部API呼び出し結果を記録する                                 |
| 19  | `raw_product_metadata`   | Raw Product Metadata   | Raw Metadata    | ○   | batch        | Object Storage上のRaw JSON参照情報を保存する                  |
| 20  | `staging_item`           | Staging Item           | 一時 / 中間     | ○   | batch        | Item反映前の商品データを保持する                              |
| 21  | `staging_item_image`     | Staging Item Image     | 一時 / 中間     | ○   | batch        | Item Image反映前の画像データを保持する                        |
| 22  | `staging_ranking_signal` | Staging Ranking Signal | 一時 / 中間     | ○   | batch        | Popularity Signal反映前のランキングデータを保持する           |
| 23  | `staging_genre`          | Staging Genre          | 一時 / 中間     | ○   | batch        | External Genre反映前のジャンルデータを保持する                |
| 24  | `staging_attribute`      | Staging Attribute      | 一時 / 中間     | △   | batch        | External Attribute反映前の属性データを保持する                |
| 25  | `product_diff_result`    | Product Diff Result    | 派生 / 判定結果 | ○   | batch        | 商品データのnew / updated / unchanged / unavailableを記録する |
| 26  | `item_import_summary`    | Item Import Summary    | Log / 集計      | ○   | batch        | Item取込結果の件数サマリを保存する                            |

### 補足

| 論点                  | 方針                                                                          |
| --------------------- | ----------------------------------------------------------------------------- |
| Raw JSON本体          | DBではなくObject Storageに保存する                                            |
| Raw Metadata          | DBに保存し、Object Storage上のRaw JSONへの参照を保持する                      |
| Staging               | 外部API形式から内部正本へ変換する中間テーブル                                 |
| Normalized Payload    | 原則DBテーブル化せず、メモリまたはStaging内のJSONとして扱う                   |
| Normalized Hash       | 差分判定用に `item` または `raw_product_metadata` / `staging_item` に保持する |
| `product_diff_result` | 長期保存が不要な場合はRetention対象にする                                     |

---

## 7. Item派生データ系テーブル一覧

| No  | テーブル名              | 論理名                | 正本区分          | MVP | 主な更新主体 | 主な用途                                                |
| --- | ----------------------- | --------------------- | ----------------- | --- | ------------ | ------------------------------------------------------- |
| 27  | `item_generation_queue` | Item Generation Queue | 状態 / Queue      | ○   | batch        | Item Semantic / Feature / Embedding再生成対象を管理する |
| 28  | `item_semantic`         | Item Semantic         | 派生              | ○   | batch        | 商品情報から抽出したSemantic Conceptを保存する          |
| 29  | `item_feature`          | Item Feature          | 派生 / 推薦用正本 | ○   | batch        | 商品Feature値を保存する                                 |
| 30  | `item_meaning`          | Item Meaning          | 派生 / 推薦用正本 | ○   | batch        | 商品のSocial / Symbolic値を保存する                     |
| 31  | `item_embedding`        | Item Embedding        | 派生 / 推薦用正本 | ○   | batch        | 商品検索・候補抽出用Embeddingを保存する                 |

### 補足

| 論点                        | 方針                                                                                                                       |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Item Feature                | Batchで事前生成し、Online推薦では参照のみとする                                                                            |
| Item Embedding              | 商品テキスト・メタデータ更新により再生成対象になる                                                                         |
| 入力hash                    | `feature_input_hash` / `embedding_input_hash` を保持する                                                                   |
| 再生成判定                  | 商品データ、Semantic Config Version、Model Version、Normalization Versionの変更を考慮する                                  |
| `item_generation_queue`     | item更新・version更新・失敗リトライ時に再生成対象を積む                                                                    |
| `item_feature` の冪等キー   | `item_id + semantic_config_version_id + feature_code + feature_input_hash + feature_normalization_version_id` を候補とする |
| `item_embedding` の冪等キー | `item_id + model_version_id + embedding_input_hash` を候補とする                                                           |

---

## 8. Semantic / Feature定義系テーブル一覧

| No  | テーブル名                 | 論理名                   | 正本区分 | MVP | 主な更新主体    | 主な用途                                                                         |
| --- | -------------------------- | ------------------------ | -------- | --- | --------------- | -------------------------------------------------------------------------------- |
| 32  | `semantic_config`          | Semantic Config          | 設定正本 | ○   | database / reco | 意味定義設定の大枠を管理する                                                     |
| 33  | `semantic_config_version`  | Semantic Config Version  | 設定正本 | ○   | database / reco | 意味定義・Featureルールのversionを管理する                                       |
| 34  | `semantic_concept`         | Semantic Concept         | 設定正本 | ○   | database / reco | 自然言語とFeatureの中間概念を管理する                                            |
| 35  | `feature_definition`       | Feature Definition       | 設定正本 | ○   | database / reco | 8次元Feature定義を管理する                                                       |
| 36  | `semantic_rule`            | Semantic Rule            | 設定正本 | ○   | database / reco | 入力文・商品情報からConceptを抽出するルールを管理する                            |
| 37  | `relationship_rule`        | Relationship Rule        | 設定正本 | ○   | database / reco | RelationshipからFeature基準値を生成する                                          |
| 38  | `occasion_rule`            | Occasion Rule            | 設定正本 | ○   | database / reco | OccasionからFeature基準値を生成する                                              |
| 39  | `pair_rule`                | Pair Rule                | 設定正本 | ○   | database / reco | Pair、すなわち Relationship × Occasion の組み合わせに対するFeature補正を管理する |
| 40  | `concept_feature_rule`     | Concept Feature Rule     | 設定正本 | ○   | database / reco | Semantic ConceptからFeature補正値を生成する                                      |
| 41  | `input_type_rule`          | Input Type Rule          | 設定正本 | ○   | database / reco | 好み・避けたい・NGなど入力種別ごとの適用方法を管理する                           |
| 42  | `feature_integration_rule` | Feature Integration Rule | 設定正本 | ○   | database / reco         | 複数Feature入力の統合ルールを管理する                                            |
| 43  | `normalization_rule`       | Normalization Rule       | 設定正本 | ○   | database / batch / reco | 意味定義 version ごとの正規化方式と正規化パラメータ version の binding を管理する |

### 補足

| 論点                                | 方針                                                                                                                                               |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `semantic_config_version`           | Feature生成・正規化・Social/Symbolic射影の意味体系versionとして扱う                                                                                |
| Public 参照（Semantic Config）      | `config_name`（親 `semantic_config` 正本）+ `version_label`（本テーブル）の composite。api（IF-DB-API-005）はアプリ層 JOIN で解決。DB ビュー・version 表への `config_name` denormalize は行わない |
| MVP 初期 `version_label`            | semver 形式 `v1.0.0`（`semantic_config_version_テーブル定義書` §17.1 参照）                                                                      |
| `model_version`との違い             | `semantic_config_version` は意味の作り方、`model_version` はRanking・Embedding・理由生成等のモデルversion                                          |
| Feature軸                           | MVPでは8次元固定                                                                                                                                   |
| `feature_rule`                      | 論理ER上の抽象名。物理テーブルでは `relationship_rule` / `occasion_rule` / `pair_rule` / `concept_feature_rule` 等へ分解する                       |
| NG条件                              | Feature化せずHard Filterで扱う                                                                                                                     |
| budget条件                          | Feature化せずHard Filterで扱う                                                                                                                     |
| `pair_master` と `pair_rule` の違い | `pair_master` は Relationship × Occasion の組み合わせ自体を管理するマスタであり、`pair_rule` はそのPairに対してFeature補正値を定義するルールである |
| `concept_feature_rule` 物理参照 | 論理 `concept_code` は物理列 `semantic_concept_id`（`semantic_concept` FK）。API `conceptCode` は JOIN 導出。`feature_delta` は 0.0〜1.0（大きさ）、`polarity` で方向表現。MVP seed は 18 Concept 対象の稀疏投入（`concept_feature_rule_テーブル定義書` §17.1） |
| `normalization_rule` と `feature_normalization_version` | `normalization_rule` は意味 version への **binding**（方式 + version 参照）。`feature_normalization_version_id` は **物理 FK ON**。sigmoid パラメータ正本（`parameter_json`）は `feature_normalization_version`（`normalization_rule_テーブル定義書` §5.1 / §8） |

---

## 9. Master / Config系テーブル一覧

|  No | テーブル名                      | 論理名                        | 正本区分 | MVP | 主な更新主体            | 主な用途                                             |
| --: | ------------------------------- | ----------------------------- | -------- | --- | ----------------------- | ---------------------------------------------------- |
|  44 | `relationship_master`           | Relationship Master           | 設定正本 | ○   | database / api          | 贈答相手Relationshipマスタ                           |
|  45 | `occasion_master`               | Occasion Master               | 設定正本 | ○   | database / api          | 贈答Occasionマスタ                                   |
|  46 | `pair_master`                   | Pair Master                   | 設定正本 | ○   | database / api / reco   | Relationship × Occasion の有効な組み合わせを管理する |
|  47 | `model_version`                 | Model Version                 | 設定正本 | ○   | database / reco / batch | LLM / Embedding / Ranking等のモデルversion管理       |
|  48 | `ranking_config`                | Ranking Config                | 設定正本 | ○   | database / reco         | Ranking計算パラメータを管理する                      |
|  49 | `reason_template`               | Reason Template               | 設定正本 | ○   | database / reco         | 推薦理由文テンプレートを管理する                     |
|  50 | `feature_normalization_version` | Feature Normalization Version | 設定正本 | ○   | database / batch / reco | Feature正規化パラメータ version を管理する           |

### 補足

| 論点                            | 方針                                                                                     |
| ------------------------------- | ---------------------------------------------------------------------------------------- |
| Relationship / Occasion         | 入力マスタ取得APIの参照元                                                                |
| Pair                            | Relationship × Occasion の有効な組み合わせを管理する。例：恋人 × 誕生日、上司 × 退職祝い |
| `pair_master` と `pair_rule`    | `pair_master` は組み合わせ定義、`pair_rule` はその組み合わせに対するFeature補正ルール    |
| `feature_normalization_version` | Item Feature / User Featureの正規化再現性を担保する                                      |
| `ranking_config`                | context_score / popularity_score / risk_score / final_score等のRanking設定を管理する     |
| `reason_template`               | reason生成のテンプレートversion管理に利用する                                            |
| `model_version`                 | embedding model / LLM / ranking model等の技術的versionを管理する                         |

---

## 10. Evaluation系テーブル一覧

| No  | テーブル名           | 論理名             | 正本区分      | MVP | 主な更新主体 | 主な用途                   |
| --- | -------------------- | ------------------ | ------------- | --- | ------------ | -------------------------- |
| 51  | `evaluation_dataset` | Evaluation Dataset | 内部正本      | △   | reco / batch | 評価データセットを管理する |
| 52  | `evaluation_case`    | Evaluation Case    | 内部正本      | △   | reco / batch | 評価ケースを管理する       |
| 53  | `evaluation_run`     | Evaluation Run     | 実行Log       | △   | reco / batch | 評価実行単位を管理する     |
| 54  | `evaluation_result`  | Evaluation Result  | 派生 / Log    | △   | reco / batch | 評価結果を保存する         |
| 55  | `evaluation_metric`  | Evaluation Metric  | 派生 / Metric | △   | reco / batch | 評価指標を保存する         |

### 補足

| 論点             | 方針                                                                   |
| ---------------- | ---------------------------------------------------------------------- |
| MVP              | 本番Online推薦の必須テーブルではないが、オフライン評価を行う場合は必要 |
| `evaluation_run` | `mode = evaluation` のRecommendation Runと関連づけ可能                 |
| 評価結果         | 将来的にランキング品質、Feature妥当性、理由文妥当性の評価に利用する    |

---

## 11. Log / Observability系テーブル一覧

| No  | テーブル名                          | 論理名                            | 正本区分 | MVP | 主な更新主体       | 主な用途                                      |
| --- | ----------------------------------- | --------------------------------- | -------- | --- | ------------------ | --------------------------------------------- |
| 56  | `batch_run_log`                     | Batch Run Log                     | Log      | ○   | batch              | Batch実行単位の開始・終了・成功失敗を記録する |
| 57  | `phase_log`                         | Phase Log                         | Log      | ○   | api / reco / batch | 処理フェーズ単位の実行記録を保存する          |
| 58  | `error_log`                         | Error Log                         | Log      | ○   | api / reco / batch | エラー発生内容を保存する                      |
| 59  | `feature_distribution_metric`       | Feature Distribution Metric       | Metric   | ○   | batch / reco       | Feature値分布を保存する                       |
| 60  | `meaning_distribution_metric`       | Meaning Distribution Metric       | Metric   | ○   | batch / reco       | Social / Symbolic分布を保存する               |
| 61  | `normalization_distribution_metric` | Normalization Distribution Metric | Metric   | ○   | batch              | 正規化前後の分布を保存する                    |
| 62  | `reco_score_distribution_metric`    | Reco Score Distribution Metric    | Metric   | △   | reco               | Matching / Rankingスコア分布を保存する        |

### 補足

| 論点                           | 方針                                                                         |
| ------------------------------ | ---------------------------------------------------------------------------- |
| `recommendation_run_phase_log` | 物理テーブルとしては作成せず、`phase_log` に統合する方針とする               |
| `api_call_log`                 | Log系だが、外部商品データ連携系でも参照するため、外部連携系一覧に配置済み    |
| Metric系                       | MVPでは専用metricテーブルを採用する。将来的に `metric_summary` へ統合可能    |
| trace                          | `trace_id` / `request_id` / `run_id` / `batch_run_id` を記録できる構造にする |
| retention                      | Log / Metric系は保持期間を物理設計で定義する                                 |
| `phase_log` 保持期間           | **90 日**（`created_at` 基準 DELETE）。Batch 系 Log 統一（Issue #536 No.10）。`phase_log_テーブル定義書` §13 |
| Batch 系 Log 保持期間          | **90 日**（`error_log` / `api_call_log` / `batch_run_log` / `item_import_summary` / `phase_log`）。BATCH-RET-001 アンカー一括パージ（Issue #536） |
| `feature_distribution_metric` 保持期間 | **365 日以上**（`calculated_at` 基準 DELETE）。`batch_run_log` 90 日パージと**非連動**（Issue #556 Human Review） |
| `feature_distribution_metric` MVP 更新主体 | **batch のみ INSERT / UPSERT**（BATCH-016）。reco は SELECT のみ（Issue #556 Human Review） |
| `reco_score_distribution_metric` 保持期間 | **365 日以上**（`calculated_at` 基準 DELETE）。`batch_run_log` 90 日パージと**非連動**（Issue #564 Human Review） |
| `reco_score_distribution_metric` MVP 更新主体 | **reco のみ INSERT / UPSERT**（Online 推薦実行時）。batch は SELECT のみ（Issue #564 Human Review） |

---

## 12. 非DB管理対象

| 対象                  | 保存先                         | テーブル化しない理由                                       |
| --------------------- | ------------------------------ | ---------------------------------------------------------- |
| Raw Product Object    | Object Storage                 | Raw JSON本体は容量が大きく、DBにはMetadataのみ保持するため |
| Normalized Payload    | memory / staging JSON          | 差分判定・変換処理中の一時データであり、正本ではないため   |
| API Response Body全文 | Object Storage / 必要時のみlog | DB肥大化を避けるため                                       |
| Feature計算途中値     | memory / 必要時debug log       | 推薦結果の再現に必要な最終値のみ保存すればよいため         |
| LLM prompt全文        | 原則非保存 / 必要時マスク保存  | 個人情報・自由入力・コスト面の懸念があるため               |

---

## 13. 作成対象テーブル総数

| 区分                     | テーブル数 |
| ------------------------ | ---------: |
| Online推薦系             |          6 |
| User意味推定系           |          3 |
| Item系                   |          7 |
| 外部商品データ連携系     |         10 |
| Item派生データ系         |          5 |
| Semantic / Feature定義系 |         12 |
| Master / Config系        |          7 |
| Evaluation系             |          5 |
| Log / Observability系    |          7 |
| 合計                     |         62 |

---

## 14. 物理ER / テーブル定義への引き継ぎ事項

| No  | 引き継ぎ事項                          | 内容                                                                                                                                 |
| --- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `ranking_snapshot` の追加             | ランキング取得単位のヘッダテーブルとして追加する                                                                                     |
| 2   | `item_popularity_signal` の冪等キー   | `ranking_snapshot_id + rank` を基本方針とする                                                                                        |
| 3   | `recommendation_run_phase_log` の扱い | `phase_log` に統合し、物理テーブルとしては作成しない                                                                                 |
| 4   | Item Feature / Embeddingの入力hash    | 商品データ更新による再生成判定のため、入力hashを保持する                                                                             |
| 5   | Feature version                       | `semantic_config_version_id` / `feature_normalization_version_id` をFeature系テーブルへ保持する                                      |
| 6   | Embedding version                     | `model_version_id` を `item_embedding` へ保持する                                                                                    |
| 7   | Review情報の扱い                      | MVPではFeature推定に使わず、Popularity補助・表示・分析用途に限定する                                                                 |
| 8   | Raw JSON本体                          | DBではなくObject Storage管理とする                                                                                                   |
| 9   | 属性データ                            | `external_attribute` / `staging_attribute` はMVP任意テーブルとして扱う                                                               |
| 10  | Metric系                              | MVPでは専用Metricテーブルを採用し、将来統合を検討する                                                                                |
| 11  | 状態更新                              | 状態カラムは終端状態を中心にDB更新し、途中経過は `phase_log` に記録する                                                              |
| 12  | テーブルスキーマ                      | MVPでは同一DB内で管理し、物理設計時に `app` / `log` / `metric` 等のschema分割を検討する                                              |
| 13  | `pair_master` の追加                  | Relationship × Occasion の有効な組み合わせを管理するマスタとして追加する                                                             |
| 14  | `pair_master` と `pair_rule` の関係   | `pair_master` は組み合わせ定義、`pair_rule` はその組み合わせに対するFeature補正ルールとして分離する                                  |
| 15  | Pair解決結果の保持                    | 推薦再現性を担保するため、`recommendation_request` または `recommendation_run` に解決済み `pair_id` を保持するかを物理設計で判断する |

---

## 15. 次工程で作成する成果物

| 順序 | 成果物         | 内容                                                                                   |
| ---- | -------------- | -------------------------------------------------------------------------------------- |
| 1    | 物理ER図       | 本テーブル一覧をもとにPK / FK / 多重度を整理する                                       |
| 2    | テーブル定義書 | 各テーブルのカラム、型、NULL可否、制約、indexを定義する                                |
| 3    | DDL            | Supabase / PostgreSQL向けSQLを作成する                                                 |
| 4    | Seed定義       | master / config / semantic_config_version / feature_definition等の初期データを定義する |
| 5    | Repository設計 | api / reco / batchからのDBアクセス単位を整理する                                       |
