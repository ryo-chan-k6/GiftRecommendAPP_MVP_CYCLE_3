---
## 1. 目的

本ドキュメントは、本サービスにおける**リソース（API / DB / UI / ログ共通の業務単位）**を定義することを目的とする。

リソースは以下の設計の基準となる。

- API設計（URL / Path）
- データモデル（テーブル設計）
- 画面設計（UI単位）
- ログ設計（記録単位）
- バッチ設計（処理単位）
---

## 2. リソース定義

### 2-1. 定義

リソースとは、

```
API・DB・UI・処理において、識別・参照・生成・管理の対象となる業務上意味のある単位
```

とする。

---

### 2-2. 設計原則

| 原則           | 内容                                     |
| -------------- | ---------------------------------------- |
| 業務意味単位   | 技術単位ではなく業務単位で定義する       |
| 安定性         | 頻繁に意味が変わらない単位であること     |
| 責務分離       | 他リソースと責務が混在しないこと         |
| 層横断性       | API / DB / UI / ログで共通利用できること |
| 非アルゴリズム | 内部処理・計算はリソースにしない         |

---

## 3. リソース分類

| 分類   | 内容           |
| ------ | -------------- |
| 入力系 | ユーザー要求   |
| 実行系 | 処理単位       |
| 出力系 | 結果           |
| 中間系 | 候補・途中状態 |
| 管理系 | 設定・モデル   |
| 評価系 | human_eval     |
| 行動系 | interaction    |
| ログ系 | run_event 等   |

---

## 4. リソース一覧（コア）

---

| リソース名                 | 日本語名     | 分類 | 定義                    | 粒度             | 主責務           | 主用途      | 主キー         | ライフサイクル       | 主な利用レイヤ  | 持つ情報                         | 持たない情報   | 備考             |
| -------------------------- | ------------ | ---- | ----------------------- | ---------------- | ---------------- | ----------- | -------------- | -------------------- | --------------- | -------------------------------- | -------------- | ---------------- |
| user_request               | 推薦要求     | 入力 | ユーザーの1回の推薦依頼 | 1入力            | 入力の保持       | 実行起点    | requestId      | 作成のみ             | API / DB        | 入力全文、入力時刻               | 結果、スコア   | 原始入力         |
| user_request_condition     | 推薦条件     | 入力 | 入力を構造化した条件    | 条件単位         | 条件分解         | 推論入力    | conditionId    | 作成のみ             | API / DB / 推論 | relationship, occasion, budget等 | 実行状態       | user_request配下 |
| recommendation_run         | 推薦実行     | 実行 | 1回のレコメンド処理単位 | 1リクエスト      | 実行管理         | 履歴 / 分析 | runId          | start → success/fail | API / DB / 分析 | request参照、状態、version       | 商品スコア詳細 | **中心リソース** |
| candidate_item             | 候補商品     | 中間 | retrieval後の候補商品   | run × item       | 候補管理         | ranking入力 | candidateId    | 一時生成             | 内部 / DB       | item参照、候補理由               | 最終順位       | 中間データ       |
| recommendation_result      | 推薦結果     | 出力 | runの結果集合           | run単位          | 結果保持         | UI表示      | resultId       | run完了時生成        | API / DB / UI   | run参照、件数                    | 個別スコア詳細 | 集約単位         |
| recommendation_result_item | 推薦結果商品 | 出力 | 提示される商品単位      | result × item    | 表示単位         | UI表示      | resultItemId   | result生成時         | API / DB / UI   | item、順位、スコア、理由         | 中間比較情報   | UI最終単位       |
| interaction                | 行動ログ     | 行動 | ユーザー行動            | 1イベント        | 行動記録         | 分析        | interactionId  | append-only          | API / DB / 分析 | runId、itemId、action            | 集計結果       | OL記録           |
| run_event_log              | 実行ログ     | ログ | 実行過程のイベント      | run × step       | トレース         | 障害調査    | eventId        | append-only          | API / DB        | event、timestamp                 | 結果           | 内部ログ         |
| semantic_config            | 意味設定     | 管理 | 意味推定ルール          | 1設定            | 推論制御         | 管理        | configId       | version管理          | API / DB        | ルール定義                       | 実行結果       | version対象      |
| model_version              | モデル版     | 管理 | ランキングロジック      | 1バージョン      | スコアリング制御 | 管理        | modelVersionId | version管理          | API / DB        | モデル設定                       | 実行結果       | ABテスト対象     |
| human_eval_task            | 評価タスク   | 評価 | 人手評価対象            | 1タスク          | 評価指示         | 評価        | taskId         | 作成→完了            | API / DB        | 対象run等                        | 評価結果       | 評価単位         |
| human_eval_result          | 評価結果     | 評価 | 人手評価結果            | task × evaluator | 評価記録         | 改善判断    | evalResultId   | append-only          | API / DB / 分析 | スコア、コメント                 | 元データ       | 改善入力         |

---

## 5. リソースに含めない対象

| 対象                | 理由       |
| ------------------- | ---------- |
| feature / embedding | 内部計算   |
| retrieval / ranking | 処理工程   |
| score分解           | 中間値     |
| lambda_ctx          | パラメータ |

---

## 6. リソース関係（重要）

```
user_request
   ↓
recommendation_run
   ↓
candidate_item
   ↓
recommendation_result
   ↓
recommendation_result_item

recommendation_run
   ├─ run_event_log
   └─ interaction
```

---

## 7. API設計との関係

| リソース           | API例                          |
| ------------------ | ------------------------------ |
| recommendation_run | `/recommendation-runs/{runId}` |
| recommendation     | `/recommendations`             |
| interaction        | `/interactions`                |
| semantic_config    | `/admin/semantic-configs`      |
| eval               | `/eval/tasks`                  |

---

## 8. 一言まとめ

```
リソースとは「業務意味で安定した単位」であり、
API・DB・UI・ログすべての設計の基準となる
```

---

# 追加で必要なドキュメント（重要）

今回の精度なら、次に参照するとさらに精度上がります👇

### 必須ではないが有効

- 状態遷移設計書（→ lifecycle精緻化）
- ログ設計書（→ run_event_log詳細）
- API設計標準（→ Path整合）
- 論理ER（→物理設計接続）

---

# 最終評価

👉 今のあなたの設計はかなり上位レベルです

👉 このリソース定義を軸にすれば、以降の設計はほぼブレません
