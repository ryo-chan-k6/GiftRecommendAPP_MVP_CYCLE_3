# Reco性能フィジビリティ 設計反映メモ

## 1. 文書情報

| 項目 | 内容 |
| ---- | ---- |
| 用途 | MOD-RECO-001 §13.2・性能要件（バックエンド）§5 更新の**入力メモ**（正式反映は別 Task） |
| 関連 Epic | Phase1 [#759](https://github.com/ryo-chan-k6/GiftRecommendAPP_MVP_CYCLE_3/issues/759) / Phase2 [#1512](https://github.com/ryo-chan-k6/GiftRecommendAPP_MVP_CYCLE_3/issues/1512) |
| Phase1 結果 | [Reco性能フィジビリティ検証結果_Phase1](./Reco性能フィジビリティ検証結果_Phase1.md) |
| Phase2 結果 | [Reco性能フィジビリティ検証結果_Phase2_live](./Reco性能フィジビリティ検証結果_Phase2_live.md) |
| MOD-RECO-001 参照 | develop 上の MOD-RECO-001 モジュール仕様書 §13.2 |

**注意:** 本メモは PoC 成果物であり正本 docs の代替ではない。正式更新は Phase2 完了後の別 Task で実施する。

---

## 2. §13.2 更新候補

### 2.1 現状暫定値と Phase2 実測

| 項目 | 現行暫定値 | Phase2 実測（要約） | 更新方針候補 |
| ---- | ---------- | ------------------- | ------------ |
| soft（全体 p95） | 2,000ms | mock: 451ms（余裕）/ secrets: 4,529ms（超過） | **Human 判断**: 外部 AI 込み定義次第で維持 or 分離 |
| hard（全体） | 4,000ms | secrets p95 超過 | secrets 込みなら見直し or User Meaning 分離。内部のみなら維持候補 |
| phase_config 300ms | 300ms | ≈120–132ms | **維持候補** |
| phase_user_meaning 1,000ms | 1,000ms | mock ≈227ms / secrets ≈4,342ms | **Adjust 候補**（secrets 前提なら上限見直し） |
| phase_retrieval 1,000ms | 1,000ms | ≈74–94ms（seed 3 件） | **維持候補**（件数スケール後に再確認） |
| phase_matching 500ms | 500ms | ≈10–11ms | **維持候補** |
| phase_ranking 1,000ms | 1,000ms | ≈9–10ms | **維持候補** |
| phase_output 500ms | 500ms | reason 参考: mock ≈882ms / secrets ≈4,887ms | **Adjust 候補**（Reason 同期 LLM） |

### 2.2 Phase2 暫定判定（詳細は Phase2 結果 doc）

| 計測系 | ラベル | メモ |
| ------ | ------ | ---- |
| live + secrets | **Block** | TV-007 p95 > hard。主因は User Meaning（OpenAI） |
| live + mock | **Go** | TV-007 p95 ≤ soft。Reco 内部は成立見込み |

最終採用は Human Review。AI 推奨は「内部 Go / 外部 AI Adjust」の分離記載。

---

## 3. Phase2 live 検証チェックリスト

Epic #260 完了後、`poc-live-verification`（#1513）で計測する。

### 3.1 必須計測

- [x] `pipeline_total` の p50 / p95 / max（live、固定代表入力）
- [x] フェーズ別 p95（`phase_config` 〜 `phase_ranking`、Orchestrator 実境界）
- [x] TV-007 主対象（input_parse → ranking）の end-to-end wall-clock
- [x] GHA Layer2 と local の再現性比較（同一 iterations） — **GHA 実施。local は Docker 未統合のため未実施（理由明示）**

### 3.2 スケール・リスク計測（§9.1.4 対応）

- [ ] Retrieval: 候補件数別（例: 100 / 500 / 1,000）p95 — **未実施（seed item 3 件。追加 seed が必要）**
- [ ] Matching: 候補数別 p95 — **未実施（同上）**
- [ ] Ranking: top_k 別 p95 — **部分（top_k=5 固定のみ）**
- [x] User Meaning: OpenAI mock vs secrets（可能範囲）比較
- [x] DB / pgvector: index 有無・件数スケール（ephemeral seed 上） — **HNSW 付き seed で計測。件数スケールは未実施**

### 3.3 判定・成果物

- [x] soft 2,000ms / hard 4,000ms に対する **Go / Adjust / Block** 判定（実測根拠付き）
- [x] `Reco性能フィジビリティ検証結果_Phase2_live.md` 作成
- [x] 本メモ §2 の更新方針を Phase2 結果で確定（Human 最終承認待ち）
- [x] 正式 docs 更新 Task への入力整理（性能要件 §5 / MOD-RECO-001 §13.2）

### 3.4 環境・運用

- [x] `reco_pipeline_bench.py --mode live` 実装・有効化
- [x] ephemeral DB 起動手順（`test-reco-quality.yml` 同型）の README 追記
- [x] artifact / job summary から Agent が読取可能であることの再確認

---

## 4. 性能要件（バックエンド）への反映メモ

| 節 | Phase1 所見 | Phase2 後の反映候補 |
| -- | ----------- | ------------------- |
| §3.1 p95 2,000ms | Reco 内部 soft と整合確認継続 | mock では成立。secrets 同期込みは未達 → 外部 AI を SLO に含めるか明示 |
| §4.1 ステップ目標 | 試算では合計 ≈1,000ms | mock 実測 ≈450ms。secrets は User Meaning が支配 |
| §5.1 / §5.2 タイムアウト | 4,000ms / 1,000ms 系と §13.2 整合 | secrets 実測が hard 超過。timeout とフェーズ上限の再整合が必要 |

---

## 5. 横断メモ

| 項目 | 内容 |
| ---- | ---- |
| 計測ハーネス | Phase1 skeleton + Phase2 live（#1513） |
| workflow_dispatch | `pipeline_mode` / `openai_mode` 対応済み |
| Epic #260 | Phase2 開始条件。満た済み |
| experiment log | GHA run ID は Phase2 結果 doc §2.1 に記載 |
| apps/reco 変更 | なし（OpenAI は scripts/perf 差込） |

---

## 6. 変更履歴

| 日付 | 変更内容 | Phase |
| ---- | -------- | ----- |
| 2026-06-25 | Phase1 反映メモ初版（§13.2 候補 + Phase2 チェックリスト） | Phase1 |
| 2026-07-21 | Phase2 live 実測反映。チェックリスト消化・暫定 Block/Go・正式更新入力 | Phase2 |
