# Reco性能フィジビリティ 設計反映メモ

## 1. 文書情報

| 項目 | 内容 |
| ---- | ---- |
| 用途 | MOD-RECO-001 §13.2・性能要件（バックエンド）§5 更新の**入力メモ**（正式反映は別 Task） |
| 関連 Epic | [#759](https://github.com/ryo-chan-k6/GiftRecommendAPP_MVP_CYCLE_3/issues/759) |
| Phase1 結果 | [Reco性能フィジビリティ検証結果_Phase1](./Reco性能フィジビリティ検証結果_Phase1.md) |
| MOD-RECO-001 参照 | develop 上の MOD-RECO-001 モジュール仕様書 §13.2 |

**注意:** 本メモは PoC 成果物であり正本 docs の代替ではない。正式更新は Phase2 完了後の別 Task で実施する。

---

## 2. Phase1 時点の §13.2 更新候補

### 2.1 現状暫定値（変更なし候補）

| 項目 | 現行暫定値 | Phase1 所見 | 更新方針候補 |
| ---- | ---------- | ----------- | ------------ |
| soft（全体 p95） | 2,000ms | 設計内訳試算では余裕あり（推論） | **Phase2 実測後に維持 or 調整** |
| hard（全体） | 4,000ms | api→reco 4s timeout と整合 | **Phase2 実測後に維持 or 調整** |
| phase_config 300ms | 300ms | skeleton のみでは未検証 | Phase2 計測後 |
| phase_user_meaning 1,000ms | 1,000ms | 同上 | Phase2 計測後 |
| phase_retrieval 1,000ms | 1,000ms | **重点確認**（pgvector） | Phase2 で超過リスク評価 |
| phase_matching 500ms | 500ms | 同上 | Phase2 計測後 |
| phase_ranking 1,000ms | 1,000ms | 同上 | Phase2 計測後 |
| phase_output 500ms | 500ms | Reason 参考。Output 本体は Phase2 | Phase2 計測後 |

### 2.2 Phase1 で正式更新しない理由

- skeleton 実測は本処理を含まない
- §13.2 の PoC 連携条項どおり、**検証後更新**が前提
- Human Review で Phase1 暫定判定（Go候補）の確認が未了

---

## 3. Phase2 live 検証で追加計測すべき項目

Epic #260 完了後、`poc-live-verification` Task で以下を計測する。

### 3.1 必須計測

- [ ] `pipeline_total` の p50 / p95 / max（live、固定代表入力）
- [ ] フェーズ別 p95（`phase_config` 〜 `phase_ranking`、Orchestrator 実境界）
- [ ] TV-007 主対象（input_parse → ranking）の end-to-end wall-clock
- [ ] GHA Layer2 と local の再現性比較（同一 iterations）

### 3.2 スケール・リスク計測（§9.1.4 対応）

- [ ] Retrieval: 候補件数別（例: 100 / 500 / 1,000）p95
- [ ] Matching: 候補数別 p95
- [ ] Ranking: top_k 別 p95
- [ ] User Meaning: OpenAI mock vs secrets（可能範囲）比較
- [ ] DB / pgvector: index 有無・件数スケール（ephemeral seed 上）

### 3.3 判定・成果物

- [ ] soft 2,000ms / hard 4,000ms に対する **Go / Adjust / Block** 判定（実測根拠付き）
- [ ] `Reco性能フィジビリティ検証結果_Phase2_live.md` 作成
- [ ] 本メモ §2 の更新方針を Phase2 結果で確定
- [ ] 正式 docs 更新 Task への入力整理（性能要件 §5 / MOD-RECO-001 §13.2）

### 3.4 環境・運用

- [ ] `reco_pipeline_bench.py --mode live` 実装・有効化
- [ ] ephemeral DB 起動手順（`test-reco-quality.yml` 同型）の README 追記
- [ ] artifact / job summary から Agent が読取可能であることの再確認

---

## 4. 性能要件（バックエンド）への反映メモ

| 節 | Phase1 所見 | Phase2 後の反映候補 |
| -- | ----------- | ------------------- |
| §3.1 p95 2,000ms | Reco 内部 soft と整合確認継続 | 実測 p95 で SLO 妥当性を記載 |
| §4.1 ステップ目標 | 試算では合計 ≈1,000ms | live 実測との差分を反映 |
| §5.1 / §5.2 タイムアウト | 4,000ms / 1,000ms 系と §13.2 整合 | 超過事例があれば調整案 |

---

## 5. 横断メモ

| 項目 | 内容 |
| ---- | ---- |
| 計測ハーネス | Phase1 で local / GHA 完走確認済み（#768, Epic push `ad30ac6`） |
| workflow_dispatch | Epic Branch 初回 push 後に利用可能（#759 検証時確認） |
| Epic #260 | Phase2 開始条件。Phase1 Epic 完了のブロッカーではない |
| experiment log | 必要時 `ai-logs/experiments/` へ GHA run ID を記録可 |

---

## 6. 変更履歴

| 日付 | 変更内容 | Phase |
| ---- | -------- | ----- |
| 2026-06-25 | Phase1 反映メモ初版（§13.2 候補 + Phase2 チェックリスト） | Phase1 |
