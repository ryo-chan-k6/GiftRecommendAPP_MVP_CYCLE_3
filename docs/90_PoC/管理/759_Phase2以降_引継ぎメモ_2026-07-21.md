# フィジビリティ検証 引継ぎメモ（2026-07-21）

> **宛先:** フィジビリティ検証を担当する別 AI Agent / Human  
> **作成:** レーン引継ぎ（Phase1 develop 取り込み完了時点）  
> **目的:** TV 全体計画と TV-007（Reco）Phase2 以降を、文脈なしで再開できるようにする

---

## 0. 30秒サマリ

| 項目 | 内容 |
| ---- | ---- |
| いま終わっていること | TV 全体マップ整備（#1496/#1497）＋ **TV-007 Phase1 が develop にマージ済**（#1502） |
| いまの焦点 | **TV-007 Phase2（live 実測）** の計画確定 → 実行 |
| 親 Epic #759 | **CLOSED**（Human が #1502 マージ時にクローズ）。Phase2 は **新 Issue 起票が必要** |
| やってはいけないこと | `apps/reco/src/**` の本格改修、正式 docs（性能要件§5 / MOD-RECO-001 §13.2）の独断更新、production 負荷、secret 実値の commit、BATCH レーンの exclusive path 横取り、PR merge |

---

## 1. 必読（この順）

1. [AGENTS.md](../../../AGENTS.md)
2. [技術検証全体計画](../計画/技術検証全体計画.md) … TV-001〜010 マップ
3. [TV-007 方針](../計画/TV要件・実施方針/TV-007_Reco性能フィジビリティ.md)
4. [Reco性能フィジビリティ検証計画書](../性能フィジビリティ/Reco性能フィジビリティ検証計画書.md)
5. [Phase1 結果](../性能フィジビリティ/Reco性能フィジビリティ検証結果_Phase1.md)
6. [設計反映メモ](../性能フィジビリティ/設計反映メモ.md) … **Phase2 チェックリストの正**
7. [759 棚卸し](./759_Reco性能フィジビリティ棚卸し_2026-07-21.md)
8. Task Definition: `prompts/definitions/tasks/reco-performance-feasibility-poc/poc-live-verification.yaml`
9. 全体テスト計画書 §7.1（TV-007）・MOD-RECO-001 §13.2・性能要件（バックエンド）§5

補助:

- `scripts/perf/README.md`
- `.github/workflows/perf-feasibility-reco.yml`
- Epic Definition: `prompts/definitions/epics/reco-performance-feasibility-poc/epic.yaml`

---

## 2. 完了済み（事実）

| 成果 | Issue / PR | develop |
| ---- | ---------- | ------- |
| `docs/90_PoC/計画/` `管理/` 新設、TV-001〜010 方針雛形 | #1496 / #1497 MERGED | あり |
| Phase1 計画・結果・設計反映メモ | #761/#763 → Epic 経由 | あり（#1502） |
| `scripts/perf/reco_pipeline_bench.py`（skeleton） | #762 | あり |
| `perf-feasibility-reco.yml` | #762 | あり |
| Definition（epic / poc-plan / harness / report / **poc-live-verification**） | Epic Branch → #1502 | あり |

Phase1 結論（要約）:

- skeleton は sub-ms（本処理なし）→ **Go/Adjust/Block の最終判定には使えない**
- Retrieval / pgvector が Phase2 の重点リスク
- soft 2,000ms / hard 4,000ms は **Phase2 実測後**に維持 or 調整

関連会話（参考・正本ではない）: Cursor agent transcript 上の UI E2E レーン完了後 → #759 棚卸し → 90_PoC 計画整備 → Phase1 land の流れ。

---

## 3. 現状の注意点（事実）

| 項目 | 状態 | 引継ぎ上の意味 |
| ---- | ---- | -------------- |
| [#759](https://github.com/ryo-chan-k6/GiftRecommendAPP_MVP_CYCLE_3/issues/759) | **CLOSED**（2026-07-21、Human、#1502 マージ時） | Phase2 を #759 子 Task としては起票できない。**新 Epic または新 Task Issue が必要** |
| `poc-live-verification.yaml` | parent が `#759` / Epic Branch 名を参照 | 新 Issue 起票時に **Definition の親 Issue・Branch を更新**すること |
| worktree `spike-epic-759-...` | ローカルに残存しうる | develop 取り込み済。再利用するか削除するかは Human/運用判断。**1 Branch = 1 worktree** |
| TV進捗一覧 | #1502 マージ直前は `Phase1取り込み中` | **本メモ反映後**に `Phase1完了（Phase2待ち）` へ更新する想定 |
| BATCH 並行レーン | 別 Agent が BATCH 実装中の可能性 | TV-008 や `apps/batch` を無断で触らない。Phase2 は主に `scripts/perf` / PoC docs / workflow。計測環境（DB・port）の取り合いに注意 |
| UI E2E soak 等 | 別レーン | 本フィジビリティと無関係。触らない |

---

## 4. 推奨する次作業（段階）

Human 確認を挟みながら進めること。

### Step A — 進捗の正本更新（小）

- [ ] `管理/TV進捗一覧.md` の TV-007 を `Phase1完了（Phase2待ち）` に更新
- [ ] 必要なら本引継ぎメモへのリンクを `管理/README.md` / `TV-007` 方針に追加

### Step B — Phase2 Issue 境界（Human 判断必須）

選択肢:

| 案 | 内容 | 推奨メモ |
| -- | ---- | -------- |
| B1 | 新 Epic「PoC:Reco性能フィジビリティ Phase2」＋ `poc-live-verification` Task | #759 CLOSED 後のきれいな切り方 |
| B2 | 識別子なし Task のみ（親 Epic なし） | 小さすぎると PR target が develop 直になりやすい。運用ルール要確認 |
| B3 | #759 を reopen して継続 | Human のみ。AI は reopen しない |

**推奨（推論）:** B1。Definition `poc-live-verification.yaml` を新 Epic に合わせて更新してから `/start-task`。

Human に確認すべきこと:

1. Phase2 の Issue 境界（B1/B2/B3）
2. live の定義（ephemeral DB + mock のみか、OpenAI secrets 実疎通を含むか）
3. TV-005 / TV-006 を Phase2 に内包するか、別 TV として先行するか
4. soft/hard に対する Go / Adjust / Block の判定ルール

### Step C — Phase2 計画の更新

- [ ] 計画書 / TV-007 方針に、現行 develop 上の Orchestrator 実装実態を反映した **live 計測境界** を書く
- [ ] 設計反映メモ §3 チェックリストを Issue の完了条件に落とす
- [ ] `reco_pipeline_bench.py --mode live` の設計（現状 skeleton 中心。live は未実装 or スタブの可能性大 → **着手時にコード確認**）

### Step D — Phase2 実行

- [ ] local live 計測
- [ ] GHA `perf-feasibility-reco`（`workflow_dispatch`、mode=live）
- [ ] 結果: `docs/90_PoC/性能フィジビリティ/Reco性能フィジビリティ検証結果_Phase2_live.md`
- [ ] 設計反映メモ更新 + Go/Adjust/Block
- [ ] **正式 docs 更新は別 Task**（out of scope）

### Step E —（任意・後続）他 TV

全体マップ上の優先候補:

1. TV-005 / TV-006（Phase2 精度向上）
2. TV-008〜010（BATCH レーンと exclusive 調整後）
3. TV-001〜004（必要時）

---

## 5. scope / path（Phase2 想定）

`poc-live-verification.yaml` 準拠（新 Issue でも大きく変えない想定）:

| 区分 | パス |
| ---- | ---- |
| 可 | `docs/90_PoC/性能フィジビリティ/**`、`docs/90_PoC/管理/**`、`docs/90_PoC/計画/**`（関連更新）、`scripts/perf/**`、`.github/workflows/perf-feasibility-*.yml`、`prompts/definitions/**/reco-performance-feasibility-poc/**` |
| 原則不可 | `apps/reco/src/**`（計測のためにどうしても必要な最小変更は **Human 確認**）、`packages/contracts/**`、`supabase/migrations/**`、`.env` 実値 |
| 正式反映 | 性能要件 §5 / MOD-RECO-001 §13.2 は **別 Task** |

判定対象（暫定）:

- soft **2,000ms** / hard **4,000ms**（MOD-RECO-001 §13.2）
- フェーズ別上限（設計反映メモ §2.1）

---

## 6. 作業手順の型（プロジェクト標準）

```text
Human 判断（Issue 境界・live 定義）
  → Task Definition 更新（親 Issue / Branch）
  → Issue 作成（bot: okuri-ai-bot）
  → Branch / worktree（develop から。旧 spike/epic-759 を無理に使い回さない方が安全）
  → 実装・docs
  → commit / push（bot）
  → PR（target は Epic Branch or develop ※Epic 有無に従う）
  → AI Review → Human Review → Human merge
```

認証:

```bash
node .github/scripts/gh-bot-auth.cjs verify
eval "$(node .github/scripts/gh-bot-auth.cjs print-setup)"
```

禁止: merge、force push to main/develop、secret 露出、BATCH レーンの奪取。

---

## 7. すぐ使えるコマンド

```bash
# 作業前チェック
pwd
git branch --show-current
git status --short
git worktree list

# Phase1 skeleton（回帰確認）
cd apps/reco
uv run python ../../scripts/perf/reco_pipeline_bench.py \
  --mode skeleton --iterations 50 --warmup 3 \
  --output-dir ../../scripts/perf/output
# 出力は gitignore: scripts/perf/output/
```

GHA: Actions → `Perf Feasibility Reco` → `workflow_dispatch`（本番経路）。Epic Branch 向け `push` トリガーは旧運用の名残。

---

## 8. Human への確認事項（着手ブロックになりうるもの）

1. Phase2 の Issue 境界（新 Epic 推奨）
2. live 計測で secrets 実疎通を許すか
3. TV-005/006 との切り分け
4. 旧 worktree / `spike/epic-759-...` Branch の削除可否
5. #759 CLOSED のままでよいか（reopen しない前提でよいか）

---

## 9. 変更しない方がよいもの（このレーン外）

- UI E2E / soak / branch protection
- BATCH-015/016 等の実装・仕様（別 Agent）
- SCR / OpenAPI / generated の横断改修

---

## 10. 改訂履歴

| 日付 | 内容 |
| ---- | ---- |
| 2026-07-21 | 初版。Phase1 develop 取り込み（#1502）完了・#759 CLOSED を前提に Phase2 引継ぎを記載 |
