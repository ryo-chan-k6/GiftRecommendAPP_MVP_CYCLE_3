schema_version: "1.0"
definition_type: "task"

task:
  id: "task-batch-local-cron-phase2-cron-cutover"
  title: "batch-local-cron-phase2:crontab載せ替え（Humanゲート）"
  summary: "Phase2配線を実crontabへ載せ替える後段Task。Phase1観測完了またはHuman明示承認まで着手禁止。AIは登録実行・--live-rakutenしない。現状はDefinitionのみでも可。"
  workstream_key: "batch-local-cron-phase2"
  phase: "cron-cutover"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]batch-local-cron-phase2:local cron Phase2親シェル配線（009〜016）"
  epic_issue_number: 1818
  epic_branch: "chore/epic-1818-batch-local-cron-phase2"
  related_issues:
    - 1745
    - 1811
    - 1798
    - 1792
    - 1607
  related_prs:
    - 1810
    - 1815

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/summarize-work"
  next:
    success: "/create-pr"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "support-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"
    - "test-ai"

background: |
  Phase1観測中に009〜016を実crontabへ載せると観測が濁るため、本TaskはHumanゲート。
  着手条件: (A) Phase1観測完了（#1811 verify相当）または (B) Human明示承認。
  AIは実crontab登録・--live-rakutenを実行しない。手順正本化・登録済み記録同期のみ。
  現時点では Definition 作成のみでもよい（/start-task はゲート解除後）。

objective: |
  Human承認後に、Phase2対応のcrontab載せ替え手順と登録済み記録を正本化し、
  親シェル経由のみの無人運用を001〜016へ拡張する。

scope:
  - "Phase2 crontab載せ替え手順の正本化（親シェルのみ・個別cron禁止）"
  - "Human向け登録チェックリスト（実行はHuman）"
  - "Human登録済み記録の同期（secretなし）"
  - "Phase1互換モード解除方針の記載（必要時）"
  - "統括#1745次本線追跡更新"

out_of_scope:
  - "AIによる実crontab登録実行"
  - "AIによる --live-rakuten 実行"
  - "design / impl / dry-run の再実施（前Task）"
  - "BATCH-018 / 019"
  - "#1792 schedule有効化 / #1607 / GHA楽天live"
  - "secret実値の表示・変更"
  - "Phase1観測中の無断載せ替え"
  - "Phase1 #1811 の勝手な完了判断（Human）"

input:
  docs:
    - path: "ai-logs/human-decisions/2026-08-01-batch-local-cron-ops-next.md"
      required: true
    - path: "docs/15_運用・改善/運用手順/local薄いオーケストレータ設計・運用手順.md"
      required: true
    - path: "docs/15_運用・改善/運用手順/local_cron_Phase1_crontab運用手順.md"
      required: true
    - path: "scripts/batch/README.md"
      required: true
  files: []

output:
  docs:
    - path: "docs/15_運用・改善/運用手順/**"
      action: "create_or_update"
      required: true
  files:
    - path: "ai-logs/human-decisions/**"
      action: "create_or_update"
      required: false

acceptance_criteria:
  - "着手前に Humanゲート条件（Phase1観測完了 or 明示承認）が満たされている"
  - "載せ替え手順docsがあり親シェル経由のみ・個別cron禁止が明記されている"
  - "Human登録済み記録がある（AIは登録実行していない）"
  - "AI live・GHA schedule・#1607・018/019・secret実値を含まない"

branch:
  no_branch: true
  summary: "phase2-cron-cutover"
  name: null
  base: null
  target: null
  worktree_required: false

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    status: "Backlog"
    phase: "15_運用・改善"
    priority: "high"
    planned_start: null
    due_date: null

issue:
  unit: "task"
  type: "chore"
  area: "batch"

dependencies:
  epics: []
  issues:
    - 1745
    - 1811
    - 1798
  prs:
    - 1810
    - 1815
  tasks:
    - "task-batch-local-cron-phase2-dry-run-verify"
  blocking: true

parallel_control:
  depends_on:
    - "task-batch-local-cron-phase2-dry-run-verify"
  blocks: []
  exclusive_files:
    - "docs/15_運用・改善/運用手順/local_cron_Phase1_crontab運用手順.md"
  conflict_risk: "high"
  contract_impact: false
  generated_impact: false
  db_impact: false

test_policy:
  required:
    - "Humanゲート条件の確認記録"
    - "登録済み記録（secretなし）"
  commands: []
  manual_checks:
    - "Phase1観測中に無断で009〜016を載せていない"
    - "AIがcrontab登録・--live-rakutenを実行していない"
  not_required:
    - "AIによる --live-rakuten 実行"
    - "AIによるcrontab登録"
  skip_reason:
    "AIによる --live-rakuten 実行": "AI禁止。Human境界"
    "AIによるcrontab登録": "Human境界"

review:
  human_review_required: true
  ai_review_required: true
  specialist_reviews:
    docs: true
    test: true
    contract: false
    security: true
  review_points:
    - "Humanゲート条件が満たされてから着手しているか"
    - "親シェル経由のみ・個別cron禁止か"
    - "AIが登録・liveしていないか"
    - "Phase1 crontabを壊していないか"
    - "secret実値がないか"

operation_logging:
  level: "standard"
  ai_logs:
    intake: false
    incidents: true
    cross_cutting: true
    experiments: true
  reason: "本番crontab変更に近い。Human判断・横断影響を記録"

human_decision_points:
  - "Phase1観測完了判定または明示承認の有無"
  - "実crontab載せ替え内容（時刻・フラグ・互換モード解除）の最終採択"
  - "載せ替え後の初回観測期間"

stop_conditions:
  - "Phase1観測未完了かつHuman明示承認がない場合（着手禁止）"
  - "AIがcrontab登録や --live-rakuten を実行する必要に見える場合"
  - "GHA schedule有効化が必要に見える場合"
  - "secret実値が必要に見える場合"

notes:
  - "現状は Definition のみ作成。/start-task は Humanゲート解除後"
  - "branch.no_branch: true（ゲート解除までBranch不要）。解除時にDefinition更新してから起票"
  - "Phase1 #1811 を勝手に完了扱いにしない"
  - "親Epic Issue #1818 / Branch chore/epic-1818-batch-local-cron-phase2"
