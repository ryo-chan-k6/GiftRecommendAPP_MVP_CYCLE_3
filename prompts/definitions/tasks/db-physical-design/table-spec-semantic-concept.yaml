schema_version: "1.0"
definition_type: "task"

task:
  id: "task-db-physical-design-table-spec-semantic-concept"
  title: "semantic_concept:Semantic Conceptテーブル定義書作成"
  summary: "物理ER・論理ER・Featureルール定義書・テーブル一覧を入力に、semantic_concept のテーブル定義書を作成する。Phase2 子 Task ③ Wave2（Semantic / Feature Rule 群）No.2。"
  phase: "06_実装設計"

work_mode: "ai-agent"

parent:
  epic_id: "epic-db-physical-design"
  epic_issue: "[Epic]db-physical-design:DB物理設計・構築"
  epic_issue_number: 435
  epic_branch: "docs/epic-435-db-physical-design"
  related_issues:
    - number: 462
      purpose: "先行 Task / Wave1 merge 済み前提"
    - number: 463
      purpose: "先行 Task / Wave1 merge 済み前提"

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "support-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  Phase2 子 Task ③ Wave2（Semantic / Feature 定義系 Rule 群）。
  Wave1（semantic_config / semantic_config_version）merge 済み前提。
  推奨着手順: feature_definition → semantic_concept → semantic_rule → relationship_rule / occasion_rule / pair_rule → concept_feature_rule → partial 2 件。

objective: |
  `semantic_concept` の物理カラム・制約・Index・更新仕様を table-spec.md テンプレ準拠で定義する。
  後続 DDL Task が migration を作成できる粒度まで確定する。

scope:
  - "table-spec.md テンプレ準拠の semantic_concept テーブル定義書作成"
  - "物理ER §8–§11・論理ER §10.2・Featureルール定義書 §17.4 前後 との突合"
  - "Task / Review Definition の整備"

out_of_scope:
  - "他テーブルのテーブル定義書"
  - "DDL・migration（Task ④⑤）"
  - "db/seeds ファイルの作成（Task ⑤）"
  - "apps/** 実装"
  - "OpenAPI / generated 変更（#469 へ委譲）"


input:
  docs:
    - path: "docs/06_実装設計/database/物理ER.md"
      required: true
      purpose: "テーブル分類・FK・Index・CHECK 方針（§8–§11）"
    - path: "docs/06_実装設計/database/enum定義書.md"
      required: true
      purpose: "enum 参照（feature_code 等）"
    - path: "docs/05_アプリケーション設計/アプリ/database/論理ER.md"
      required: true
      purpose: "§10.2 Semantic / Feature 系エンティティ属性"
    - path: "docs/05_アプリケーション設計/アプリ/database/テーブル一覧.md"
      required: true
      purpose: "§8 Semantic / Feature 定義系"
    - path: "docs/04_ドメインモデル設計/Featureルール定義書.md"
      required: true
      purpose: "Rule 定義 §17.4 前後"
    - path: "docs/06_実装設計/api/API-PUB-007_Semantic設定取得API契約仕様書.md"
      required: true
      purpose: "semanticConcepts Response マッピング"
    - path: "docs/04_ドメインモデル設計/SemanticConcept定義書.md"
      required: true
      purpose: "Concept コード・意味"
  templates:
    - path: "prompts/templates/docs/table-spec.md"
      required: true
      purpose: "成果物テンプレート"

output:
  docs:
    - path: "docs/06_実装設計/database/semantic_concept_テーブル定義書.md"
      action: "create"
      required: true
      template: "prompts/templates/docs/table-spec.md"
  files:
    - path: "prompts/definitions/tasks/db-physical-design/table-spec-semantic-concept.yaml"
      action: "create"
      required: true
    - path: "prompts/definitions/reviews/db-physical-design/table-spec-semantic-concept/pr-review.yaml"
      action: "create"
      required: true

acceptance_criteria:
  - "semantic_concept テーブル定義書が docs/06_実装設計/database/ に存在する"
  - "物理ER §8–§11・論理ER §10.2・テーブル一覧 §8 と矛盾しない"
  - "apps/** 変更がない"
  - "Task PR target が親 Epic Branch"
  - "semantic_config_version_id への FK 方針が明記されている"
  - "API-PUB-007 semanticConcepts マッピングが明記されている"

branch:
  no_branch: false
  summary: "semantic-concept-table-spec"
  base: null
  target: null
  worktree_required: true

project:
  fields:
    phase: "06_実装設計"
    status: "Todo"
    priority: "high"

issue:
  unit: "task"
  type: "docs"
  area: "db"

dependencies:
  issues:
    - number: 463
      purpose: "Wave1 semantic_config_version 完了"
  blocking: true

parallel_control:
  exclusive_files:
    - "docs/06_実装設計/database/semantic_concept_テーブル定義書.md"
  conflict_risk: "low"
  db_impact: false

test_policy:
  manual_checks:
    - "論理ER §10.2 semantic_concept 属性との整合"
    - "SemanticConcept定義書とのコード整合"
  not_required:
    - "unit test"
  skip_reason:
    "unit test": "Phase2 docs 整備 Task のため"

review:
  human_review_required: true
  ai_review_required: true
  specialist_reviews:
    docs: true

notes:
  - "Wave2 着手順 No.2"
  - "Semantic Concept 定義。API-PUB-007 semanticConcepts 表面。"
  - "OpenAPI / generated は Epic 終盤 Task #469 で一括同期"
