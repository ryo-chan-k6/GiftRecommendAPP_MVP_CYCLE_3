schema_version: "1.0"
definition_type: "task"

task:
  id: "task-db-physical-design-table-spec-relationship-rule"
  title: "relationship_rule:Relationship Ruleテーブル定義書作成"
  summary: "物理ER・論理ER・Featureルール定義書・API-PUB-008 を入力に、relationship_rule のテーブル定義書を作成する。Phase2 子 Task ③ Wave2 No.4（Semantic / Feature Rule 群）。"
  phase: "06_実装設計"

work_mode: "ai-agent"

parent:
  epic_id: "epic-db-physical-design"
  epic_issue: "[Epic]db-physical-design:DB物理設計・構築"
  epic_issue_number: 435
  epic_branch: "docs/epic-435-db-physical-design"
  related_issues:
    - number: 462
      purpose: "Wave1 semantic_config（merge 済み前提）"
    - number: 463
      purpose: "Wave1 semantic_config_version（merge 済み前提）"
    - number: 442
      purpose: "relationship_master テーブル定義書（relationship_code 参照）"
    - number: 470
      purpose: "Wave2 No.1 feature_definition テーブル定義書（feature_code 参照）"
    - number: 471
      purpose: "Wave2 No.2 semantic_concept（推奨着手順・非 blocking）"
    - number: 472
      purpose: "Wave2 No.3 semantic_rule（推奨着手順・非 blocking）"

commands:
  primary: "/work-issue"
  allowed:
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/create-pr"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "support-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  Phase2 子 Task ③ Wave2 No.4（Semantic / Feature Rule 群）。
  Wave1（semantic_config / semantic_config_version）merge 済み前提。
  推奨着手順: feature_definition → semantic_concept → semantic_rule → relationship_rule / occasion_rule / pair_rule → concept_feature_rule → partial 2 件。
  Featureルール定義書 §17.1 および API-PUB-008 `baseValueRules[]`（`ruleType: relationship`）の DB 正本を確定する。

objective: |
  `relationship_rule` の物理カラム・制約・Index・更新仕様を table-spec.md テンプレ準拠で定義する。
  後続 DDL Task が migration を作成できる粒度まで確定する。

scope:
  - "table-spec.md テンプレ準拠の relationship_rule テーブル定義書作成"
  - "物理ER §8–§11・論理ER §10.2・Featureルール定義書 §17.1 との突合"
  - "API-PUB-008 baseValueRules（ruleType: relationship）との DB 列マッピング整理"
  - "Task / Review Definition の整備"

out_of_scope:
  - "他テーブルのテーブル定義書"
  - "DDL・migration（Task ④⑤）"
  - "db/seeds ファイルの作成（Task ⑤）"
  - "apps/** 実装"
  - "OpenAPI / generated 変更（#469 へ委譲）"

input:
  docs:
    - path: "docs/06_実装設計/database/物理ER.md"
      required: true
      purpose: "テーブル分類・FK・Index・CHECK 方針（§8–§11）"
    - path: "docs/06_実装設計/database/enum定義書.md"
      required: true
      purpose: "feature_code / relationship_code enum 参照"
    - path: "docs/05_アプリケーション設計/アプリ/database/論理ER.md"
      required: true
      purpose: "§10.2 Semantic / Feature 系エンティティ属性"
    - path: "docs/05_アプリケーション設計/アプリ/database/テーブル一覧.md"
      required: true
      purpose: "§8 Semantic / Feature 定義系"
    - path: "docs/04_ドメインモデル設計/Featureルール定義書.md"
      required: true
      purpose: "Rule 定義 §17.1（relationship_rule 論理項目）"
    - path: "docs/06_実装設計/api/API-PUB-008_Featureルール取得API契約仕様書.md"
      required: true
      purpose: "baseValueRules[]（ruleType: relationship）Response マッピング"
    - path: "docs/06_実装設計/api/API-PUB-005_Relationshipマスタ取得API契約仕様書.md"
      required: true
      purpose: "relationshipCode コード体系整合"
    - path: "docs/06_実装設計/database/semantic_config_version_テーブル定義書.md"
      required: true
      purpose: "semantic_config_version_id FK 方針"
    - path: "docs/06_実装設計/database/relationship_master_テーブル定義書.md"
      required: true
      purpose: "relationship_code LOGICAL 参照方針"
    - path: "docs/06_実装設計/database/feature_definition_テーブル定義書.md"
      required: true
      purpose: "feature_code 参照・MVP 8 軸整合"
  templates:
    - path: "prompts/templates/docs/table-spec.md"
      required: true
      purpose: "成果物テンプレート"

output:
  docs:
    - path: "docs/06_実装設計/database/relationship_rule_テーブル定義書.md"
      action: "create"
      required: true
      template: "prompts/templates/docs/table-spec.md"
  files:
    - path: "prompts/definitions/tasks/db-physical-design/table-spec-relationship-rule.yaml"
      action: "create"
      required: true
    - path: "prompts/definitions/reviews/db-physical-design/table-spec-relationship-rule/pr-review.yaml"
      action: "create"
      required: true

acceptance_criteria:
  - "relationship_rule テーブル定義書が docs/06_実装設計/database/ に存在する"
  - "物理ER §8–§11・論理ER §10.2・テーブル一覧 §8 と矛盾しない"
  - "Featureルール定義書 §17.1 の論理項目が物理カラムとして整理されている"
  - "API-PUB-008 baseValueRules（ruleType: relationship）の DB 列マッピングが明記されている"
  - "relationship_master.relationship_code / feature_definition.feature_code との参照方針が明記されている"
  - "apps/** 変更がない"
  - "Task PR target が親 Epic Branch"

branch:
  no_branch: false
  summary: "relationship-rule-table-spec"
  base: "docs/epic-435-db-physical-design"
  target: "docs/epic-435-db-physical-design"
  worktree_required: true

project:
  fields:
    phase: "06_実装設計"
    status: "Todo"
    priority: "high"

issue:
  number: 473
  unit: "task"
  type: "docs"
  area: "db"

dependencies:
  issues:
    - number: 462
      purpose: "Wave1 semantic_config 完了"
    - number: 463
      purpose: "Wave1 semantic_config_version 完了"
    - number: 442
      purpose: "relationship_master テーブル定義書 完了（relationship_code 参照）"
    - number: 470
      purpose: "Wave2 No.1 feature_definition テーブル定義書 完了（feature_code 参照）"
  blocking: true

parallel_control:
  exclusive_files:
    - "docs/06_実装設計/database/relationship_rule_テーブル定義書.md"
  conflict_risk: "low"
  db_impact: false

test_policy:
  manual_checks:
    - "物理ER §8–§11 とカラム・制約・Index の整合"
    - "Featureルール定義書 §17.1 / §20.1（12 分類 × 8 軸）との整合"
    - "API-PUB-008 baseValueRules（relationshipCode / featureCode / featureBaseValue）マッピング"
    - "relationship_master / feature_definition 参照方針（LOGICAL FK + CHECK）"
    - "apps 実装層への変更がないこと"
  not_required:
    - "unit test"
  skip_reason:
    "unit test": "Phase2 docs 整備 Task のため"

review:
  human_review_required: true
  ai_review_required: true
  specialist_reviews:
    docs: true

notes:
  - "Wave2 着手順 No.4: relationship_rule"
  - "MVP: Relationship 12 分類 × Feature 8 軸の基準値 Rule（Featureルール定義書 §20.1）"
  - "API-PUB-008 baseValueRules[] 表面。OpenAPI / generated は Epic 終盤 Task #469 で一括同期"
  - "occasion_rule / pair_rule テーブル定義 Task との Rule 群一貫性を Human Review で確認"
