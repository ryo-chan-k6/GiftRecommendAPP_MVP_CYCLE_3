schema_version: "1.0"
definition_type: "task"

task:
  id: "task-batch-parent-schedule-batch-017-gha-live-db"
  title: "batch-parent-schedule:BATCH import連鎖GHA live化（案C3）"
  summary: "案 C3。017 + 上流 import（003→005→006→007→008→017）を GHA live（stg）化する。共有 pipeline_batch_run_id と葉ごと job_run_id を分離する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]batch-parent-schedule:親workflow schedule・dry-run有効化"
  epic_issue_number: 1637
  epic_branch: "feature/epic-1637-batch-parent-schedule"
  related_issues:
    - 1715
  related_prs: []

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"
    - "test-ai"

background: |
  Human 確定: 案 C3。Environment stg Secrets（STG_DATABASE_URL / RAKUTEN_*）を用い、
  Object Storage は未登録のため --live-object-storage は付けない（scaffold storage）。
  prod 禁止。secret 実値を docs/コード/ログに出さない。

objective: |
  BATCH import 連鎖（003→005→006→007→008→017）の GHA を scaffold demo から
  stg live 経路へ切り替え、Run ID を共有 pipeline UUID + 葉ごと job_run_id に分離する。

scope:
  - "Task / Review Definition"
  - "CLI: 003/005/006 の job_run_id / batch_run_id 分離（017 と同型）"
  - "親 batch-rakuten-item-import.yml の pipeline UUID 出力と葉への受け渡し"
  - "葉 workflow live 化（003/005/006/007/008/017）environment: stg"
  - "運用メモ（最小）または Issue コメント用サマリ"
  - "関連 UT 実行・必要なら最小追加"

out_of_scope:
  - "ranking_snapshot / meaning-generation / existing-item-pipeline 全体"
  - "Object Storage live 有効化（Secrets 未登録）"
  - "schedule 有効化"
  - "production"
  - "#1715 worktree の編集"
  - "PR 作成 / push（本 Task は commit まで）"

input:
  docs:
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ親workflow_schedule有効化ギャップ一覧.md"
      required: false
  files:
    - path: ".github/workflows/batch-rakuten-item-import.yml"
      required: true
    - path: ".github/workflows/batch-import-summary.yml"
      required: true
    - path: "apps/batch/src/batch/application/import_summary/__main__.py"
      required: true

output:
  files:
    - path: "prompts/definitions/tasks/batch-parent-schedule/batch-017-gha-live-db.yaml"
      action: "create"
      required: true
    - path: "prompts/definitions/reviews/batch-parent-schedule/batch-017-gha-live-db/pr-review.yaml"
      action: "create"
      required: false
    - path: "apps/batch/src/batch/application/item_pseudo_diff/__main__.py"
      action: "update"
      required: true
    - path: "apps/batch/src/batch/application/raw_staging/__main__.py"
      action: "update"
      required: true
    - path: "apps/batch/src/batch/application/product_diff/__main__.py"
      action: "update"
      required: true
    - path: ".github/workflows/batch-rakuten-item-import.yml"
      action: "update"
      required: true
    - path: ".github/workflows/batch-rakuten-item-pseudo-diff.yml"
      action: "update"
      required: true
    - path: ".github/workflows/batch-rakuten-raw-staging.yml"
      action: "update"
      required: true
    - path: ".github/workflows/batch-rakuten-product-diff.yml"
      action: "update"
      required: true
    - path: ".github/workflows/batch-rakuten-item-apply.yml"
      action: "update"
      required: true
    - path: ".github/workflows/batch-rakuten-item-active-status.yml"
      action: "update"
      required: true
    - path: ".github/workflows/batch-import-summary.yml"
      action: "update"
      required: true
  docs:
    - path: "docs/15_運用・改善/運用手順/BATCH_import連鎖_GHA_live化メモ_C3.md"
      action: "create"
      required: false

acceptance_criteria:
  - "親 resolve-run-id が UUID の pipeline_batch_run_id を出力する"
  - "葉が environment: stg と STG_DATABASE_URL を用い --scaffold-demo を外している"
  - "003 は RAKUTEN_* + BATCH_RAKUTEN_LIVE（または --live-rakuten）。--live-object-storage なし"
  - "003/005/006 CLI が --job-run-id（葉）と --batch-run-id（共有）を分離する"
  - "017 live 時に job_run_id ≠ batch_run_id となる"
  - "secret 実値が docs/コード/ログに含まれない"
  - "指定 UT が通る"
  - "Related to / PR target = 親 Epic Branch（PR 作成時）"

branch:
  no_branch: false
  summary: "batch-017-gha-live-db"
  base: "feature/epic-1637-batch-parent-schedule"
  target: "feature/epic-1637-batch-parent-schedule"
  worktree_required: true

issue:
  unit: "task"
  type: "feature"
  area: "batch"
  number: 1717
