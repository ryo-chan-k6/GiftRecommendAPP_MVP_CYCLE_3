schema_version: "1.0"
definition_type: "task"

task:
  id: "task-web-foundation-common-ui-components"
  title: "共通UIコンポーネント骨格"
  summary: "apps/web/src/components 共通 UI 骨格。W2。"
  phase: "06_実装設計"

work_mode: "ai-agent"

parent:
  epic_id: "epic-web-foundation"
  epic_issue: "[Epic]web-foundation:Web共通基盤"
  epic_issue_number: 724
  epic_branch: "feature/epic-724-web-foundation"
  related_issues: []
  related_prs: []

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "test-ai"
  review:
    - "reviewer-ai"
    - "test-ai"

background: |
  Phase4a Epic `web-foundation` 子 Task。Phase3b 完了後、apps/web/src/components 共通 UI 骨格。W2。
  Phase4b 縦串（MOD-RECO / API-INT / API-PUB / SCR）の前提となる共通基盤の一部。

objective: |
  `apps/web/src/components/**` を Phase4a scope 内で整備し、単体テスト可能な骨格を提供する。

scope:
  - "骨格コード・ディレクトリ・最小テスト"
  - "関連 docs 追記（必要最小）"
  - "Task / Review Definition 整備"

out_of_scope:
  - "Phase4b 識別子 Epic 本格実装"
  - "packages/contracts/** 変更・generated 手編集"
  - "Layer2 workflow 変更"
  - "secret 実値の commit"

input:
  docs:
    - path: "docs/00_共通/プロジェクト管理/実装フェーズ実行プロセス設計書.md"
      required: true
      purpose: "Phase4a §6.5"
    - path: "docs/05_アプリケーション設計/アプリ/web/UIコンポーネント一覧.md"
      required: true
      purpose: "共通 UI 部品定義"
    - path: "docs/05_アプリケーション設計/アプリ/web/デザインルール.md"
      required: true
      purpose: "スタイル・部品ルール"

output:
  files:
    - path: "apps/web/src/components/**"
      action: "create_or_update"
      required: true

acceptance_criteria:
  - "Phase4a scope 内骨格が存在する"
  - "packages/contracts / generated を手編集していない"
  - "secret 実値が含まれていない"
  - "Task PR target が親 Epic Branch"

branch:
  no_branch: false
  summary: "common-ui-components"
  worktree_required: true

project:
  fields:
    phase: "06_実装設計"
    status: "Todo"
    priority: "high"

issue:
  unit: "task"
  type: "feature"
  area: "web"

dependencies:
  blocking: true

parallel_control:
  depends_on:
    - "prompts/definitions/tasks/web-foundation/design-system-rules.yaml"
  exclusive_files:
    - "apps/web/src/components/**"
  conflict_risk: "medium"
  blocks:
    []

test_policy:
  manual_checks:
    - "scope 内 lint / unit（可能な範囲）"

review:
  human_review_required: true
  ai_review_required: true
  specialist_reviews:
    docs: true
    test: true

contract_gate:
  required: false

notes:
  - "親 Epic `/start-epic` 後に着手"
