schema_version: "1.0"
definition_type: "task"

task:
  id: "task-batch-011-feature-input-hash-batch-spec"
  title: "BATCH-011:Feature入力hash算出バッチ仕様書作成"
  summary: "BATCH-011（Feature入力hash算出Batch）のバッチ仕様書を作成し、IF-DB-BATCH-012・payload/hash 方針・Queue 境界を正本化する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]BATCH-011:Feature入力hash算出Batch"
  epic_issue_number: 1434
  epic_branch: "feature/epic-1434-batch-011-feature-input-hash"
  related_issues: []
  related_prs: []

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "orchestrator-ai"
    - "docs-reviewer-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  Phase4b Feature 入力 hash 段。Epic #1434 bootstrap 済み。BATCH-010 develop merge 済み。
  専用仕様書は未着手。Contract Gate 不要。

objective: |
  BATCH-011 バッチ仕様書を作成し、後続実装・UT の入力正本とする。

scope:
  - "BATCH-011_Feature入力hash算出バッチ仕様書.md 新規作成"
  - "IF-DB-BATCH-012 / IF-DB-BATCH-011 混同防止"
  - "feature_input_payload / canonicalize / SHA-256"
  - "独立 workflow 提案"
  - "Task / Review Definition"

out_of_scope:
  - "Python 実装・workflow YAML 本体・UT"
  - "BATCH-012 / apps/reco / migration"
  - "親 meaning-generation 全体改修"

input:
  docs:
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ処理一覧.md"
      required: true
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ設計方針書.md"
      required: true
    - path: "docs/06_実装設計/database/item_feature_テーブル定義書.md"
      required: true
    - path: "docs/06_実装設計/batch/BATCH-010_Item Semantic生成バッチ仕様書.md"
      required: true

output:
  docs:
    - path: "docs/06_実装設計/batch/BATCH-011_Feature入力hash算出バッチ仕様書.md"
      action: "create"
      required: true
  files:
    - path: "prompts/definitions/tasks/batch-011-feature-input-hash/batch-spec.yaml"
      action: "create"
      required: true
    - path: "prompts/definitions/reviews/batch-011-feature-input-hash/batch-spec/pr-review.yaml"
      action: "create"
      required: true

deliverables:
  - "BATCH-011 バッチ仕様書"
  - "Task / Review Definition"

acceptance_criteria:
  - "仕様書が作成されている"
  - "IF-DB-BATCH-012 と IF-DB-BATCH-011 の区別が明記されている"
  - "§13.3 入力 ○/× と整合している"
  - "§18 で確定 / 提案が区別されている"
  - "secret 非含有"
  - "PR target が親 Epic Branch"

branch:
  no_branch: false
  name: "docs/task-1435-batch-011-feature-input-hash-batch-spec"
  summary: "batch-011-feature-input-hash-batch-spec"
  base: "feature/epic-1434-batch-011-feature-input-hash"
  target: "feature/epic-1434-batch-011-feature-input-hash"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "07_開発・単体テスト"
    status: "In Progress"
    priority: "high"

issue:
  number: 1435
  unit: "task"
  type: "docs"
  area: "batch"

dependencies:
  epics:
    - issue: 1434
      title: "[Epic]BATCH-011:Feature入力hash算出Batch"
      purpose: "親 Epic"

contract_gate:
  required: false

parallel_control:
  exclusive_files:
    - "docs/06_実装設計/batch/BATCH-011_*"
  conflict_risk: "low"
  generated_impact: false
  contract_impact: false
  db_impact: false

test_policy:
  required:
    - "docs review"
    - "secret check"
  commands: []
  not_required:
    - "unit test"
  skip_reason:
    "unit test": "docs Task"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "IF-DB-BATCH-012 / IF-DB-BATCH-011 混同がないか"
    - "item_feature 書込が BATCH-012 側と明記されているか"
    - "§13.3 との整合"
  specialist_reviews:
    docs: true
    security: true

operation_logging:
  level: "standard"
  ai_logs:
    intake: false
    incidents: true
    cross_cutting: false
    experiments: false

human_decision_points:
  - "独立 YAML（batch-feature-input-hash.yml）採否"
  - "skip stub / config キー採否"

stop_conditions:
  - "正本 docs 間で矛盾があり独断解消できない場合"

notes:
  - "Issue #1435"
