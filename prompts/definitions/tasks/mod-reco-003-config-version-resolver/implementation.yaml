schema_version: "1.0"
definition_type: "task"

task:
  id: "task-mod-reco-003-config-version-resolver-implementation"
  title: "MOD-RECO-003:Config Version Resolver実装"
  summary: "MOD-RECO-003（Config Version Resolver）のモジュール仕様書に基づき、`apps/reco/src/reco/application/config-version-resolver/**` にモジュール本体を実装する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]MOD-RECO-003:Config Version Resolver"
  epic_issue_number: null
  epic_branch: "feature/epic-<issue-number>-mod-reco-003-config-version-resolver"
  related_issues: []
  related_prs: []

commands:
  primary: "/work-issue"
  allowed:
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/create-pr"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "orchestrator-ai"
    - "support-ai"
    - "test-ai"
  review:
    - "reviewer-ai"

background: |
  Epic 配下で MOD-RECO-003 モジュール仕様書 Task 完了後、モジュール本体を `apps/reco/src/reco/application/config-version-resolver/**` に実装する。
  Orchestrator（MOD-RECO-001）のポート契約に適合する実装とする。
  配線方針は `MOD-RECO-001` §8.4（3 段階ハイブリッド）に従う。起動フェーズ（002/003）は `stubs.py` へ本実装配線可。それ以外のフェーズ Wiring は別 Task。

objective: |
  MOD-RECO-003 モジュール本体を実装し、モジュール仕様書の責務・入出力・例外方針を満たす。
  単体テストの網羅的追加は後続 unit-test Task に委譲する。

scope:
  - "`apps/reco/src/reco/application/config-version-resolver/**` へのモジュール本体実装"
  - "Orchestrator ポート（ConfigResolverPort）に適合する公開 I/F"
  - "Orchestrator 統合テスト（明示 DI、`test_orchestrator_integration.py`）"
  - "起動フェーズ Wiring：`recommendation-orchestrator/stubs.py` の `config_resolver` を本実装へ（`MOD-RECO-001` §8.4.2）"
  - "必要最小限の domain / infrastructure 利用（epic_scope 内）"
  - "実装スモークテスト（最小限）"

out_of_scope:
  - "起動フェーズ以外の Orchestrator スタブ差し替え（フェーズ Wiring Task）"
  - "API-INT-002 エンドポイント層"
  - "他 Recoモジュール本体"
  - "OpenAPI / DB schema 変更"
  - "網羅的単体テスト（unit-test Task）"

input:
  docs:
    - path: "docs/06_実装設計/reco/MOD-RECO-003_Config Version Resolverモジュール仕様書.md"
      required: true
      purpose: "実装正本"
    - path: "docs/06_実装設計/reco/MOD-RECO-001_Recommendation Orchestratorモジュール仕様書.md"
      required: true
      purpose: "ポート契約・呼び出し関係"
    - path: "docs/05_アプリケーション設計/アプリ/reco/Recoモジュール一覧.md"
      required: true
      purpose: "モジュール責務境界"
  templates: []
  files:
    - path: "apps/reco/src/reco/application/recommendation-orchestrator/ports.py"
      required: true
      purpose: "下位モジュールポート契約"
  issues: []
  prs: []

output:
  docs: []
  files:
    - path: "apps/reco/src/reco/application/config-version-resolver/**"
      action: "create"
      required: true
  tests: []
  generated:
    expected: false
    paths: []
    handling: "none"
  logs:
    ai_logs_required: false
    path: null

deliverables:
  - "MOD-RECO-003 モジュール本体実装"
  - "最小スモークテスト（任意・実装 PR 内）"

acceptance_criteria:
  - "`apps/reco/src/reco/application/config-version-resolver/**` に実装がある"
  - "モジュール仕様書と矛盾しない"
  - "Orchestrator 統合テスト（`test_orchestrator_integration.py`）が存在する"
  - "起動フェーズ：`build_default_stub_ports` が `ConfigVersionResolver` を参照する"
  - "apps/reco/src/reco/api/** を変更していない"
  - "他 MOD 本体ディレクトリを変更していない"
  - "secret、APIキー、.env 実値が含まれていない"

branch:
  no_branch: false
  name: "feature/task-<issue-number>-mod-reco-003-config-version-resolver-implementation"
  base: "feature/epic-<issue-number>-mod-reco-003-config-version-resolver"
  target: "feature/epic-<issue-number>-mod-reco-003-config-version-resolver"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "07_開発・単体テスト"
    status: "Todo"
    priority: "high"

issue:
  unit: "task"
  type: "feature"
  area: "reco"

dependencies:
  epics: []
  issues: []
  prs: []
  tasks:
    - "MOD-RECO-003 モジュール仕様書 Task が Human Review 完了していること"
  blocking: true

parallel_control:
  depends_on:
    - "task-mod-reco-003-config-version-resolver-module-spec"
  exclusive_files:
    - "apps/reco/src/reco/application/config-version-resolver/**"
  conflict_risk: "medium"
  contract_impact: false
  generated_impact: false
  db_impact: false

test_policy:
  required:
    - "typecheck"
    - "unit test（最小スモーク）"
    - "secret check"
  commands:
    - "cd apps/reco && python -m compileall src/reco/application/config-version-resolver"
  not_required:
    - "integration test"
  skip_reason:
    "integration test": "下位モジュール単体のため後続または API-INT 接続後"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "モジュール仕様書との整合"
    - "Orchestrator ポート契約との整合"
    - "epic_scope 内に差分が収まっているか"
  specialist_reviews:
    docs: false
    test: false
    contract: false
    security: false

operation_logging:
  level: "standard"

notes:
  - "Definition bootstrap 由来スキャフォールド。"
  - "workstream: mod-reco-003-config-version-resolver"
