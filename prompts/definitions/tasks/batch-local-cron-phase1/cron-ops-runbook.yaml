schema_version: "1.0"
definition_type: "task"

task:
  id: "task-batch-local-cron-phase1-cron-ops-runbook"
  title: "batch-local-cron-phase1:crontab運用手順と定常ノブ同期"
  summary: "local cron Phase1（001〜008）のcrontab運用手順を正本化し、定常ノブをDecisionへ同期する。実crontab登録手順は書くが実行はHuman。AIは --live-rakuten を実行しない。"
  workstream_key: "batch-local-cron-phase1"
  phase: "ops-runbook"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]batch-local-cron-phase1:local cron Phase1無人運用（001〜008）"
  epic_issue_number: 1811
  epic_branch: "chore/epic-1811-batch-local-cron-phase1"
  related_issues:
    - 1745
    - 1798
    - 1792
    - 1607
  related_prs:
    - 1810

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/summarize-work"
  next:
    success: "/create-pr"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "support-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  前提Epic #1798 / PR #1810 で local薄いオーケストレータと本格収集枠は develop 済み。
  Decision 2026-08-01-batch-local-cron-ops-next により次本線は local cron Phase1。
  本Taskは「どう登録し・どのノブで回すか」を docs / Decision に正本化する。
  実crontab登録・PC常時起動・観測期間の --live-rakuten は Human。
  AIは手順記載・Decision同期・scripts/batchの最小整合修正のみ。

objective: |
  crontab運用手順と定常ノブを正本docs / Human Decision に同期し、
  Humanが安全にcron登録できる状態にする。

scope:
  - "crontab運用手順の正本化（親シェル経由のみ・個別cron禁止・ログパス・失敗時の見方）"
  - "定常ノブ（ジャンル・予算・停止条件・017任意扱い）の Decision 同期"
  - "Human向け実crontab登録チェックリスト（実行はHuman）"
  - "scripts/batch README / 運用手順の最小整合"
  - "後続 cron-ops-verify の着手条件（Human登録済み）を明記"

out_of_scope:
  - "実crontab登録の実行（Human）"
  - "数日無人観測の実施・結果docs本体（後続 cron-ops-verify）"
  - "AIによる --live-rakuten 実行"
  - "BATCH-009〜016配線"
  - "BATCH-018 / 019"
  - "#1792 schedule有効化 / #1607 / GHA楽天live"
  - "secret実値の表示・変更"
  - "OpenAPI / generated / apps/web|api|reco"
  - "親orchestrator workflowのscheduleコメント解除"

input:
  docs:
    - path: "ai-logs/human-decisions/2026-08-01-batch-local-cron-ops-next.md"
      required: true
    - path: "docs/15_運用・改善/運用手順/local薄いオーケストレータ設計・運用手順.md"
      required: true
    - path: "docs/15_運用・改善/運用手順/楽天Fetch運用方針.md"
      required: true
    - path: "docs/15_運用・改善/運用手順/batch-data-collect-ops_local継続収集結果_1808.md"
      required: true
    - path: "scripts/batch/README.md"
      required: true
  files: []

output:
  docs:
    - path: "docs/15_運用・改善/運用手順/**"
      action: "create_or_update"
      required: true
  files:
    - path: "ai-logs/human-decisions/**"
      action: "create_or_update"
      required: true
    - path: "scripts/batch/**"
      action: "update"
      required: false

acceptance_criteria:
  - "crontab運用手順docsがあり、親シェル経由のみ・個別cron禁止が明記されている"
  - "定常ノブが Human Decision（または同等正本）に同期されている"
  - "Human登録チェックリストがあり、AIが登録実行していないことが分かる"
  - "GHA schedule有効化・#1607・GHA楽天live・009〜016・018/019・secret実値を含まない"
  - "後続 verify Task の着手条件（Human登録済み）が明記されている"

branch:
  no_branch: false
  summary: "cron-ops-runbook"
  name: "chore/task-1813-cron-ops-runbook"
  base: "chore/epic-1811-batch-local-cron-phase1"
  target: "chore/epic-1811-batch-local-cron-phase1"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    status: "In Progress"
    phase: "15_運用・改善"
    priority: "high"
    planned_start: "2026-08-01"
    due_date: "2026-08-03"

issue:
  number: 1813
  unit: "task"
  type: "chore"
  area: "batch"

dependencies:
  epics: []
  issues:
    - 1745
    - 1798
  prs:
    - 1810
  tasks: []
  blocking: false

parallel_control:
  depends_on: []
  blocks:
    - "task-batch-local-cron-phase1-cron-ops-verify"
  exclusive_files:
    - "docs/15_運用・改善/運用手順/local薄いオーケストレータ設計・運用手順.md"
    - "scripts/batch/README.md"
  conflict_risk: "low"
  contract_impact: false
  generated_impact: false
  db_impact: false

test_policy:
  required:
    - "docs整合確認（手順・Decision・個別cron禁止）"
  commands: []
  manual_checks:
    - "手順が親シェル（local_daily / local_weekly）のみをcrontab対象としている"
    - "secret実値・scheduleコメント解除を含まない"
  not_required:
    - "AIによる --live-rakuten 実行"
  skip_reason:
    "AIによる --live-rakuten 実行": "本Taskは手順正本化。liveはHuman"

review:
  human_review_required: true
  ai_review_required: true
  specialist_reviews:
    docs: true
    test: false
    contract: false
    security: true
  review_points:
    - "親シェル経由のみ・個別cron禁止が明記されているか"
    - "定常ノブがDecisionと矛盾しないか"
    - "AIがcrontab登録や --live-rakuten を実行していないか"
    - "GHA schedule / #1607 / Phase2(009〜016) が混入していないか"
    - "secret実値がないか"

human_decision_points:
  - "定常ノブの最終採択（継続/変更）"
  - "実crontab登録の実施タイミング"

stop_conditions:
  - "実crontab登録や --live-rakuten をAIが実行する必要に見える場合"
  - "GHA schedule有効化が必要に見える場合"
  - "secret実値が必要に見える場合"
  - "009〜016配線が本Taskに必要に見える場合"

notes:
  - "Task Issue #1813 / Branch chore/task-1813-cron-ops-runbook / 親Epic #1811"
  - "依存: #1798 CLOSED / Decision 2026-08-01-batch-local-cron-ops-next decided"
  - "後続: cron-ops-verify（Human登録後）"
  - "Projects追加は bot PAT に project scope がなく未実施（Human依頼）"
