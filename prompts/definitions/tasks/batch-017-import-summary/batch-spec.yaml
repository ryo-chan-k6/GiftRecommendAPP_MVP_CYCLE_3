schema_version: "1.0"
definition_type: "task"

task:
  id: "task-batch-017-import-summary-batch-spec"
  title: "BATCH-017:Import Summary作成バッチ仕様書作成"
  summary: "BATCH-017（Import Summary作成Batch）のバッチ仕様書を作成し、IF-DB-BATCH-017（item_import_summary INSERT）・IF-DB-BATCH-016 / IF-OBS-006 境界・冪等・集計対応・workflow / MOD-BATCH-047 を正本化する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]BATCH-017:Import Summary作成Batch"
  epic_issue_number: 1499
  epic_branch: "feature/epic-1499-batch-017-import-summary"
  related_issues: []
  related_prs: []

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "orchestrator-ai"
    - "docs-reviewer-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  Phase4b 運用集計段。Epic #1499 bootstrap 済み。先行 BATCH-016 は develop merge 済み。
  物理書込 IF = IF-DB-BATCH-017（Batch ID と IF 番号が一致）。IF-DB-BATCH-016 / IF-OBS-006 混同禁止。
  冪等キー batch_run_id + source_api。MVP は INSERT + ON CONFLICT DO NOTHING（UPDATE しない）。
  独立 cron なし。Contract Gate 不要。Retention 90 日。MOD-BATCH-047 正（Builder 同義）。

objective: |
  BATCH-017 バッチ仕様書を作成し、後続実装・UT の入力正本とする。

scope:
  - "BATCH-017_Import Summary作成バッチ仕様書.md 新規作成"
  - "IF-DB-BATCH-017 / IF-DB-BATCH-016 / IF-OBS-006 境界の明記"
  - "集計元と件数列の対応・source_api 0 固定・Feature/Embedding 同一 Run 完了時のみ"
  - "冪等 INSERT DO NOTHING・独立 cron なし・workflow §18 Human・MOD-BATCH-047"
  - "Task / Review Definition"

out_of_scope:
  - "Python 実装・workflow YAML 本体・UT"
  - "BATCH-016 再実装 / BATCH-018 以降"
  - "apps/reco 破壊変更 / migration / 親チェーン全体改修"
  - "Public / Internal API / OpenAPI 変更"

input:
  docs:
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ処理一覧.md"
      required: true
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ設計方針書.md"
      required: true
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ依存関係図.md"
      required: true
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ実行スケジュール設計書.md"
      required: true
    - path: "docs/05_アプリケーション設計/アプリ/インターフェース一覧.md"
      required: true
    - path: "docs/05_アプリケーション設計/アプリ/モジュール一覧.md"
      required: true
    - path: "docs/06_実装設計/database/item_import_summary_テーブル定義書.md"
      required: true
    - path: "docs/06_実装設計/batch/BATCH-016_分布メトリクス集計バッチ仕様書.md"
      required: true
    - path: "docs/06_実装設計/batch/BATCH-015_Item Embedding生成バッチ仕様書.md"
      required: true
    - path: "prompts/definitions/epics/batch-017-import-summary/epic.yaml"
      required: true

output:
  docs:
    - path: "docs/06_実装設計/batch/BATCH-017_Import Summary作成バッチ仕様書.md"
      action: "create"
      required: true
  files:
    - path: "prompts/definitions/tasks/batch-017-import-summary/batch-spec.yaml"
      action: "create"
      required: true
    - path: "prompts/definitions/reviews/batch-017-import-summary/batch-spec/pr-review.yaml"
      action: "create"
      required: true

deliverables:
  - "BATCH-017 バッチ仕様書"
  - "Task / Review Definition"

acceptance_criteria:
  - "仕様書が作成されている"
  - "IF-DB-BATCH-017 = BATCH-017（item_import_summary）と IF-DB-BATCH-016 / IF-OBS-006 の区別が明記されている"
  - "冪等が batch_run_id + source_api、MVP は INSERT + ON CONFLICT DO NOTHING である"
  - "集計元と件数列の対応・ranking/genre 0 固定・Feature/Embedding 同一 Run 完了時のみ（未完了 0）が明記されている"
  - "独立 cron なし・workflow 独立子および/または distribution-metrics 017 step（§18 Human）が明記されている"
  - "MOD-BATCH-047 正・Builder 同義・Contract Gate 不要・Public API 非公開・secret 禁止・Retention 90 日が明記されている"
  - "§18.1 確定と §18.2 残未決が区別されている"
  - "PR target が親 Epic Branch"

branch:
  no_branch: false
  name: "docs/task-1500-batch-017-import-summary-batch-spec"
  summary: "batch-017-import-summary-batch-spec"
  base: "feature/epic-1499-batch-017-import-summary"
  target: "feature/epic-1499-batch-017-import-summary"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "07_開発・単体テスト"
    status: "In Progress"
    priority: "high"

issue:
  number: 1500
  unit: "task"
  type: "docs"
  area: "batch"

dependencies:
  epics:
    - issue: 1499
      title: "[Epic]BATCH-017:Import Summary作成Batch"
      purpose: "親 Epic"

contract_gate:
  required: false

parallel_control:
  exclusive_files:
    - "docs/06_実装設計/batch/BATCH-017_*"
  conflict_risk: "low"
  generated_impact: false
  contract_impact: false
  db_impact: false

test_policy:
  required:
    - "docs review"
    - "secret check"
  commands: []
  not_required:
    - "unit test"
  skip_reason:
    "unit test": "docs Task"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "IF-DB-BATCH-017 が本 Batch の物理書込（item_import_summary）である"
    - "IF-DB-BATCH-016 / IF-OBS-006 混同がないか"
    - "冪等 INSERT + ON CONFLICT DO NOTHING（UPDATE しない）"
    - "集計対応・source_api 0 固定・Feature/Embedding 同一 Run 完了時のみ"
    - "独立 cron なし・workflow §18 Human・MOD-BATCH-047"
    - "Contract Gate 不要 / Public API 非公開 / secret 禁止 / Retention 90 日"
    - "§18.1 確定と §18.2 残未決の区別"
    - "secret / Public API 非公開"
  specialist_reviews:
    docs: true
    security: true

operation_logging:
  level: "standard"
  ai_logs:
    intake: false
    incidents: true
    cross_cutting: false
    experiments: false

human_decision_points:
  - "独立子 batch-import-summary.yml 中心か、batch-distribution-metrics.yml への 017 step 追加を正とするか（推奨: 併用）"
  - "親 import / meaning-generation 末尾への 017 追加を本 Epic に含めるか後続 Task か"
  - "Feature/Embedding 件数タイミング（同一 Run 未完了時 0）をテーブル定義どおり確定するか"
  - "パッケージ名 import_summary と MOD-BATCH-047 / Builder 同義の最終確認"

stop_conditions:
  - "正本 docs 間で矛盾があり独断解消できない場合"

notes:
  - "Epic #1499"
  - "Issue #1500"
  - "§18.2 は Human 判断待ち。推奨案は仕様書に記載"
