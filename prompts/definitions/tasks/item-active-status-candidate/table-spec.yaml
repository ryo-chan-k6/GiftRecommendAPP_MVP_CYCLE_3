schema_version: "1.0"
definition_type: "task"

task:
  id: "task-item-active-status-candidate-table-spec"
  title: "item_active_status_candidate:テーブル定義書作成"
  summary: "item_active_status_candidate テーブル定義書を product_diff_result 同型で作成し、BATCH-004 §18.1.1 制約を DDL 可能な粒度へ落とす。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]item_active_status_candidate:active_status候補テーブル整備"
  epic_issue_number: 1227
  epic_branch: "feature/epic-1227-item-active-status-candidate"
  related_issues:
    - number: 1228
      purpose: "T1 横断 docs 整合（merged）"
    - number: 1222
      purpose: "BATCH-004 親 Epic（スタック先）"

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "orchestrator-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  T1 で横断 docs へ追記済み。本 Task でテーブル定義書と candidate_status enum 正本を作成し、T3 DDL の入力とする。

objective: |
  item_active_status_candidate_テーブル定義書.md を作成し、UNIQUE / Retention / Writer·Reader / enum を確定する。

scope:
  - "docs/06_実装設計/database/item_active_status_candidate_テーブル定義書.md の新規作成"
  - "candidate_status の code-definition / enum定義書追記"
  - "BATCH-004 §18.1.1 制約の転記・DDL 粒度化"

out_of_scope:
  - "migration / DDL SQL（T3 #1230）"
  - "Batch Writer / Applier 実装"
  - "インターフェース一覧 IF 追加（T4b）"

input:
  docs:
    - path: "docs/06_実装設計/batch/BATCH-004_楽天既存商品再確認バッチ仕様書.md"
      required: true
      purpose: "§18.1 No.7 / §18.1.1"
    - path: "docs/06_実装設計/database/product_diff_result_テーブル定義書.md"
      required: true
      purpose: "同型先例"
  templates: []
  files: []
  issues: []
  prs: []

output:
  docs:
    - path: "docs/06_実装設計/database/item_active_status_candidate_テーブル定義書.md"
      action: "create"
      required: true
    - path: "docs/06_実装設計/database/enum定義書.md"
      action: "update"
      required: true
  files:
    - path: "packages/code-definitions/state/item_active_status_candidate_status.yaml"
      action: "create"
      required: true
  tests: []
  generated:
    expected: false
    paths: []
    handling: "none"
  logs:
    ai_logs_required: false
    path: null

deliverables:
  - "テーブル定義書"
  - "candidate_status enum 正本"

acceptance_criteria:
  - "UNIQUE が batch_run_id + source + external_item_code である"
  - "candidate_status と Retention が §18.1.1 と矛盾しない"
  - "Writer=BATCH-004 / Reader=BATCH-008 / Raw 非載荷が明記されている"
  - "migration SQL を含まない"
  - "secret 非含有"

branch:
  no_branch: false
  name: "docs/task-1229-item-active-status-candidate-table-spec"
  summary: "item-active-status-candidate-table-spec"
  base: "feature/epic-1227-item-active-status-candidate"
  target: "feature/epic-1227-item-active-status-candidate"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "06_実装設計"
    status: "In Progress"
    priority: "high"
    planned_start: "2026-07-15"
    due_date: "2026-07-17"

issue:
  unit: "task"
  type: "docs"
  area: "db"

dependencies:
  epics:
    - issue: 1227
      title: "[Epic]item_active_status_candidate:active_status候補テーブル整備"
      purpose: "親 Epic"
  issues:
    - 1228
  prs: []
  blocking: false

contract_gate:
  required: false

parallel_control:
  depends_on:
    - "task-item-active-status-candidate-docs-align"
  blocks:
    - "task-item-active-status-candidate-ddl"
  exclusive_files:
    - "docs/06_実装設計/database/item_active_status_candidate_テーブル定義書.md"
  conflict_risk: "medium"
  generated_impact: false
  contract_impact: false
  db_impact: true

test_policy:
  required:
    - "secret check"
  not_required:
    - "unit test"
  skip_reason:
    "unit test": "定義書 Task のため対象外"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "§18.1.1 整合"
    - "product_diff_result 同型との整合"
    - "DDL 可能な粒度か"
    - "migration 混入がないか"
  specialist_reviews:
    docs: true
    test: false
    contract: false
    security: true

operation_logging:
  level: "standard"
  ai_logs:
    intake: false
    incidents: true
    cross_cutting: true
    experiments: false
  reason: "DB 物理定義の横断影響"

stop_conditions:
  - "§18.1.1 と矛盾するカラム方針が必要になった場合"

notes:
  - "Issue #1229"
