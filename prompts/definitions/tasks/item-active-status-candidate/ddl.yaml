schema_version: "1.0"
definition_type: "task"

task:
  id: "task-item-active-status-candidate-ddl"
  title: "item_active_status_candidate:DDL作成"
  summary: "T2 テーブル定義書に基づき item_active_status_candidate の db/ddl と supabase migration を作成する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]item_active_status_candidate:active_status候補テーブル整備"
  epic_issue_number: 1227
  epic_branch: "feature/epic-1227-item-active-status-candidate"
  related_issues:
    - number: 1229
      purpose: "T2 テーブル定義書（merged）"

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "orchestrator-ai"
  review:
    - "reviewer-ai"
    - "docs-reviewer-ai"

background: |
  T2 でテーブル定義書と candidate_status enum が確定済み。本 Task で CREATE TABLE / Index / CHECK / UNIQUE を migration 化する。

objective: |
  db/ddl/d16_item_active_status_candidate.sql と supabase/migrations を作成し、§18.1.1 と整合させる。

scope:
  - "db/ddl/d16_item_active_status_candidate.sql の新規作成"
  - "supabase/migrations/*_item_active_status_candidate.sql の新規作成"
  - "UNIQUE / CHECK / Index の反映"

out_of_scope:
  - "Batch アプリ実装（T4a / T6）"
  - "cleanup Job（T7）"
  - "インターフェース一覧（T4b）"
  - "物理 FK の追加"

input:
  docs:
    - path: "docs/06_実装設計/database/item_active_status_candidate_テーブル定義書.md"
      required: true
      purpose: "DDL 正本"
    - path: "docs/06_実装設計/batch/BATCH-004_楽天既存商品再確認バッチ仕様書.md"
      required: true
      purpose: "§18.1.1 制約"
  templates: []
  files:
    - path: "db/ddl/d15_metric_log.sql"
      purpose: "DDL ファイル形式の参照"
  issues: []
  prs: []

output:
  docs: []
  files:
    - path: "db/ddl/d16_item_active_status_candidate.sql"
      action: "create"
      required: true
    - path: "supabase/migrations/20260715120000_item_active_status_candidate.sql"
      action: "create"
      required: true
  tests: []
  generated:
    expected: false
    paths: []
    handling: "none"
  logs:
    ai_logs_required: false
    path: null

deliverables:
  - "D16 DDL"
  - "supabase migration"

acceptance_criteria:
  - "物理名 item_active_status_candidate の CREATE TABLE がある"
  - "UNIQUE (batch_run_id, source, external_item_code) がある"
  - "candidate_status / candidate_active_status CHECK がある"
  - "物理 FK を追加していない"
  - "secret 非含有"

branch:
  no_branch: false
  name: "feature/task-1230-item-active-status-candidate-ddl"
  summary: "item-active-status-candidate-ddl"
  base: "feature/epic-1227-item-active-status-candidate"
  target: "feature/epic-1227-item-active-status-candidate"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "06_実装設計"
    status: "In Progress"
    priority: "high"
    planned_start: "2026-07-15"
    due_date: "2026-07-17"

issue:
  unit: "task"
  type: "feature"
  area: "db"

dependencies:
  epics:
    - issue: 1227
      title: "[Epic]item_active_status_candidate:active_status候補テーブル整備"
      purpose: "親 Epic"
  issues:
    - 1229
  prs: []
  blocking: false

contract_gate:
  required: false

parallel_control:
  depends_on:
    - "task-item-active-status-candidate-table-spec"
  blocks:
    - "task-batch-004-item-active-status-candidate-writer"
  exclusive_files:
    - "db/ddl/d16_item_active_status_candidate.sql"
    - "supabase/migrations/20260715120000_item_active_status_candidate.sql"
  conflict_risk: "medium"
  generated_impact: false
  contract_impact: false
  db_impact: true

test_policy:
  required:
    - "secret check"
  commands: []
  not_required:
    - "unit test"
    - "supabase migration apply"
  skip_reason:
    "unit test": "DDL 追加のみ。適用検証はローカル/CI 方針に従う"
    "supabase migration apply": "本番適用は Human / 運用判断"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "テーブル定義書 §6〜§10 との整合"
    - "§18.1.1 UNIQUE / Retention 方針との矛盾がないか"
    - "db/ddl と supabase/migrations の内容一致"
    - "物理 FK 非追加"
  specialist_reviews:
    docs: false
    test: false
    contract: false
    security: true

operation_logging:
  level: "standard"
  ai_logs:
    intake: false
    incidents: true
    cross_cutting: true
    experiments: false
  reason: "DB schema 追加"

stop_conditions:
  - "テーブル定義書と矛盾する DDL 変更が必要になった場合"

notes:
  - "Issue #1230"
