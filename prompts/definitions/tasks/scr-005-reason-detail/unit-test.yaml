schema_version: "1.0"
definition_type: "task"

task:
  id: "task-scr-005-reason-detail-unit-test"
  title: "SCR-005:推薦理由詳細表示単体テスト"
  summary: "SCR-005（推薦理由詳細表示）実装に対し、画面仕様 §18 の未カバー観点（初期折りたたみ・同時展開・高さ上限）を拡充する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]SCR-005:推薦理由詳細表示"
  epic_issue_number: 1389
  epic_branch: "feature/epic-1389-scr-005-reason-detail"
  related_issues:
    - number: 1404
      purpose: "本 Task Issue"
    - number: 1389
      purpose: "親 Epic"
    - number: 1402
      purpose: "前提 Task（実装）"
  related_prs:
    - number: 1405
      purpose: "本 Task PR（単体テスト）"
    - number: 1403
      purpose: "実装 Task PR（親 Epic Branch merge 済み）"

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/review-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "test-ai"
  support:
    - "worker-ai"
    - "support-ai"
  review:
    - "reviewer-ai"
    - "test-ai"

background: |
  SCR-005 Epic（#1389）の単体テスト Task。implementation Task（#1402 / PR #1403）merge 済み。
  実装時点で ReasonDetailPanel の主要テストはあるが、§18 の初期折りたたみ・同時展開・高さ上限を拡充する。

objective: |
  SCR-005 画面仕様 §18 の主要観点を再現可能に検証する。

scope:
  - "apps/web/tests/component/recommendation-result/** の拡充"
  - "初期折りたたみ / 同時展開 / 高さ上限クラス"
  - "Task / Review Definition 整備"

out_of_scope:
  - "画面仕様書・実装本体の機能追加（軽微なテスト容易性修正は最小限可）"
  - "E2E / Playwright"
  - "OpenAPI / generated 変更"
  - "apps/api / apps/reco / apps/batch"

input:
  docs:
    - path: "docs/06_実装設計/web/SCR-005_推薦理由詳細表示画面仕様書.md"
      required: true
      purpose: "テスト観点の正本（§18）"
  templates: []
  files:
    - path: "apps/web/src/features/recommendation-result/**"
      required: true
      purpose: "テスト対象"
  issues:
    - number: 1402
      purpose: "実装 Task"
  prs:
    - number: 1403
      purpose: "実装 PR"

output:
  docs: []
  files: []
  tests:
    - path: "apps/web/tests/component/recommendation-result/**/*.{test,spec}.{ts,tsx}"
      action: "create_or_update"
      required: true
  generated:
    expected: false
    paths: []
    handling: "none"
  logs:
    ai_logs_required: false
    path: null

deliverables:
  - "SCR-005 単体テスト拡充"
  - "画面仕様 §18 主要観点の再現可能な検証"

acceptance_criteria:
  - "初期折りたたみ・同時展開・高さ上限のテストが追加されている"
  - "pnpm --filter @gift-recommendation/web test:component が通る"
  - "epic_scope.allowed_paths 外へ変更していない"
  - "secret / .env 実値を含んでいない"
  - "本番 network に依存していない"

branch:
  no_branch: false
  name: "test/task-1404-scr-005-reason-detail-unit-test"
  base: "feature/epic-1389-scr-005-reason-detail"
  target: "feature/epic-1389-scr-005-reason-detail"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "07_開発・単体テスト"
    status: "Todo"
    priority: "high"
    planned_start: "2026-07-17"
    due_date: "2026-07-18"

issue:
  unit: "task"
  type: "test"
  area: "web"

dependencies:
  epics:
    - issue: 1389
      title: "[Epic]SCR-005:推薦理由詳細表示"
      purpose: "親 Epic"
  issues:
    - number: 1402
      purpose: "実装 Task"
  prs:
    - number: 1403
      purpose: "実装 PR"
  tasks:
    - "implementation Task が親 Epic Branch に merge 済みであること"
  blocking: true

parallel_control:
  depends_on:
    - "implementation"
  blocks: []
  exclusive_files:
    - "apps/web/tests/component/recommendation-result/**"
  conflict_risk: "low"
  generated_impact: false
  contract_impact: false
  db_impact: false

test_policy:
  required:
    - "unit test"
  commands:
    - "pnpm --filter @gift-recommendation/web test:component"
  manual_checks:
    - "画面仕様書 §18 の主要観点がテストでカバーされていること"
  not_required:
    - "e2e"
  skip_reason:
    "e2e": "本 Task は単体テスト範囲"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "画面仕様 §18 の未カバー観点が追加されているか"
    - "不安定な network / 本番依存がないか"
    - "secret 非含有か"
  specialist_reviews:
    docs: false
    test: true
    contract: false
    security: false

operation_logging:
  level: "standard"
  ai_logs:
    intake: false
    incidents: true
    cross_cutting: false
    experiments: false
  reason: "通常のテスト Task。作業計画は Issue、レビューは PR を正本とする。"

stop_conditions:
  - "implementation が未完了の場合"
  - "テストが本番 API / 本番 secret に依存する場合"

notes:
  - "Issueタイトル: [Task]SCR-005:推薦理由詳細表示単体テスト"
  - "本 Task Issue: #1404 / Branch: test/task-1404-scr-005-reason-detail-unit-test"
  - "本 Task PR: #1405"
  - "親Epic: #1389 / Branch: feature/epic-1389-scr-005-reason-detail"
  - "PR 本文は Related to #1404（Closes 禁止）"
  - "Review Definition: prompts/definitions/reviews/scr-005-reason-detail/unit-test/pr-review.yaml"
  - "前提: #1402 / PR #1403 merge 済み"
