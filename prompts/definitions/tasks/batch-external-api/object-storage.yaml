schema_version: "1.0"
definition_type: "task"

task:
  id: "task-batch-external-api-object-storage"
  title: "batch-external-api:Object Storage client（IF-STG）"
  summary: "S3CompatibleObjectStorageClient と create_object_storage_client（live 既定 off）を追加し、BATCH-001〜005 に配線する。"

work_mode: "ai-agent"

parent:
  epic_issue: "[Epic]batch-external-api:外部API本接続"
  epic_issue_number: 1598
  epic_branch: "feature/epic-1598-batch-external-api"
  related_issues:
    - 1610
    - 1611
  related_prs:
    - 1611

commands:
  primary: "/start-task"
  allowed:
    - "/start-task"
    - "/work-issue"
    - "/create-pr"
    - "/fix-review-comments"
    - "/summarize-work"
  next:
    success: "/work-issue"
    review_fix: "/fix-review-comments"
    blocked: null

agent:
  primary: "worker-ai"
  support:
    - "test-ai"
  review:
    - "reviewer-ai"
    - "test-ai"

background: |
  楽天・Embedding 本接続完了。Object Storage は Scaffold のみ。001〜004 は live-rakuten 時も Storage Scaffold。

objective: |
  IF-STG-001/002（put/get）の S3 互換 HTTP client を Scaffold 切替付きで導入し、001〜005 が明示 live 時に本接続できる。

scope:
  - "S3CompatibleObjectStorageClient（path-style + SigV4・httpx）"
  - "create_object_storage_client（live 既定 off）"
  - "secret マスク"
  - "BATCH-001〜004 / 005 CLI（--live-object-storage）"
  - "UT（httpx mock）"
  - "ギャップ一覧 T4 進捗"
  - "Task / Review Definition"

out_of_scope:
  - "delete / list / multipart"
  - "Protocol 拡張"
  - "boto3 追加"
  - "CI 既定 live / production 無承認 live"
  - "005 DB SELECT 本配線"
  - "Semantic LLM"
  - "#1607 egress"

input:
  docs:
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ外部API本実装ギャップ一覧.md"
      required: true
    - path: "docs/06_実装設計/cross_cutting/環境設計書.md"
      required: true
  files:
    - path: "apps/batch/src/batch/infrastructure/object_storage/**"
      required: true
    - path: "apps/batch/src/batch/application/genre_sync/__main__.py"
      required: true
    - path: "apps/batch/src/batch/application/raw_staging/__main__.py"
      required: true

output:
  docs:
    - path: "docs/05_アプリケーション設計/アプリ/batch/バッチ外部API本実装ギャップ一覧.md"
      action: "update"
      required: true
  files:
    - path: "apps/batch/src/batch/infrastructure/object_storage/**"
      action: "update"
      required: true
    - path: "apps/batch/src/batch/application/**/__main__.py"
      action: "update"
      required: true
    - path: "apps/batch/tests/unit/**"
      action: "update"
      required: true

deliverables:
  - "S3CompatibleObjectStorageClient / create_object_storage_client"
  - "001〜005 CLI 配線"
  - "UT / ギャップ一覧"

acceptance_criteria:
  - "live 既定 off で Scaffold"
  - "put/get が Protocol を満たす"
  - "secret 非露出"
  - "Related to（Closes 禁止）/ PR target が親 Epic"

branch:
  no_branch: false
  name: "feature/task-1612-batch-external-api-object-storage"
  summary: "batch-external-api-object-storage"
  base: "feature/epic-1598-batch-external-api"
  target: "feature/epic-1598-batch-external-api"
  worktree_required: true

project:
  project_name: "Gift Recommendation Service MVP Cycle 3"
  fields:
    phase: "07_開発・単体テスト"
    status: "In Progress"
    priority: "high"
    planned_start: "2026-07-25"
    due_date: "2026-07-29"

issue:
  number: 1612
  unit: "task"
  type: "feature"
  area: "batch"

dependencies:
  epics:
    - issue: 1598
      purpose: "親 Epic"
  issues:
    - number: 1610
      purpose: "T3 完了後の次 Task"
  blocking: false

contract_gate:
  required: false

parallel_control:
  exclusive_files:
    - "apps/batch/src/batch/infrastructure/object_storage/**"
    - "apps/batch/src/batch/application/genre_sync/__main__.py"
    - "apps/batch/src/batch/application/ranking_snapshot/__main__.py"
    - "apps/batch/src/batch/application/item_pseudo_diff/__main__.py"
    - "apps/batch/src/batch/application/item_recheck/__main__.py"
    - "apps/batch/src/batch/application/raw_staging/__main__.py"
  conflict_risk: "medium"
  db_impact: false

test_policy:
  required:
    - "unit test"
    - "secret check"
  not_required:
    - "CI live Object Storage"
  optional:
    - "local 明示 live（件数限定）"

review:
  human_review_required: true
  ai_review_required: true
  review_points:
    - "live 既定 off"
    - "secret 非露出"
    - "S3 互換 / OBJECT_STORAGE_*"
    - "Related to / PR target"
  specialist_reviews:
    test: true
    security: true

human_decision_points: []

notes:
  - "環境設計書の OBJECT_STORAGE_*（S3 互換）を正とする。httpx + SigV4（boto3 なし）"
  - "005 は Storage live まで。DB SELECT は維持して exit 3"
