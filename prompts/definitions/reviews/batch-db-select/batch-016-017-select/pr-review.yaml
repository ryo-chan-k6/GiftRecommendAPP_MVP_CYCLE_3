schema_version: "1.0"
definition_type: "review"

review:
  id: "review-batch-db-select-016-017-select-pr"
  title: "batch-db-select BATCH-016-017 SELECT PRレビュー"
  summary: "Wave G（最終 SELECT）として BATCH-016 / 017 の監視・集計 SELECT 本配線 Task の PR を AI Review する。"
  type: "task_pr_review"
  status: "ready"

work_mode: "ai-agent"

branch:
  worktree_required: true

target:
  pr:
    number: 1656
  issue:
    number: 1653
  task_definition: "prompts/definitions/tasks/batch-db-select/batch-016-017-select.yaml"
  parent_epic_issue: "[Epic]batch-db-select:DB読取本配線"
  parent_epic_branch: "feature/epic-1623-batch-db-select"

commands:
  primary: "/review-pr"

agent:
  primary: "reviewer-ai"
  specialist:
    test: "test-ai"
    security: null

review_scope:
  docs: true
  source: true
  tests: true
  api_contract: false
  db: true
  generated: false
  cicd: false
  security: true
  project_operation: true

review_points:
  - "016 load_item_features / load_item_meanings（任意 embedding）が DbReader を使うか"
  - "017 require_batch_run / load_api_calls / load_diffs / load_staging_items が DbReader を使うか"
  - "Metric UPSERT / item_import_summary INSERT 本格化・E4・LLM を混入していないか"
  - "非 demo で DATABASE_URL 無しは exit 2 か（exit 3 解除）"
  - "実 DB 疎通結果が PR に記録されているか（secret なし）"
  - "scaffold-demo / UT 回帰"
  - "Related to / PR target が親 Epic"

notes:
  - "Wave G（最終 SELECT Wave）。書込本格化 / E4 / user_meaning JOIN / DbReader OR-JOIN 拡張 / Epic PR→develop は out of scope"
  - "Epic PR → develop は本 Task merge 後の別作業"
