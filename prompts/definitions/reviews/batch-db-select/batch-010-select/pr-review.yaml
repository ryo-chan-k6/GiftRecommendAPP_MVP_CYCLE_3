schema_version: "1.0"
definition_type: "review"

review:
  id: "review-batch-db-select-010-select-pr"
  title: "batch-db-select BATCH-010 SELECT PRレビュー"
  summary: "Wave D として BATCH-010 の queue/item SELECT 本配線 Task の PR を AI Review する。"
  type: "task_pr_review"
  status: "ready"

work_mode: "ai-agent"

branch:
  worktree_required: true

target:
  pr:
    number: null
  issue:
    number: 1644
  task_definition: "prompts/definitions/tasks/batch-db-select/batch-010-select.yaml"
  parent_epic_issue: "[Epic]batch-db-select:DB読取本配線"
  parent_epic_branch: "feature/epic-1623-batch-db-select"

commands:
  primary: "/review-pr"

agent:
  primary: "reviewer-ai"
  specialist:
    test: "test-ai"
    security: null

review_scope:
  docs: true
  source: true
  tests: true
  api_contract: false
  db: true
  generated: false
  cicd: false
  security: true
  project_operation: true

review_points:
  - "list_claimable_queues / load_item / find_item_semantic が DbReader を使うか"
  - "LLM / 書込本格化（claim / UPSERT SQL formalization）を混入していないか"
  - "source フィルタが item 経由 in-process（JOIN なし）か"
  - "非 demo で DATABASE_URL 無しは exit 2 か（exit 3 解除）"
  - "実 DB 疎通結果が PR に記録されているか（secret なし）"
  - "scaffold-demo / UT 回帰"
  - "Related to / PR target が親 Epic"

notes:
  - "Wave D。Semantic LLM 本接続 / Config Version Resolver 実 DB / genre JOIN は out of scope"
