schema_version: "1.0"
definition_type: "contract"

contract:
  id: "contract-api-pub-003-item-detail-openapi-fragment"
  title: "API-PUB-003:商品詳細取得OpenAPI断片反映"
  summary: "API-PUB-003（GET /api/v1/items/{itemId}）の契約仕様書に基づき public-api.yaml へ OpenAPI 断片を追加する。"
  type: "api_contract"
  priority: "high"
  status: "ready"

work_mode: "ai-agent"

source:
  discovered_from: "phase1_wave2"
  related_issue: "#399"
  related_pr: "#405"
  related_task_definition: "prompts/definitions/tasks/api-pub-003-item-detail/api-contract-spec.yaml"
  reason: "Phase1 1b 直列 merge（#367）。#415（INT-001）完了後の次手。"

change:
  api_name: "商品詳細取得"
  api_kind: "public"
  endpoint: "GET /api/v1/items/{itemId}"
  method: "GET"
  change_type: "add_endpoint"
  breaking_change: false

scope:
  - "public-api.yaml に GET /api/v1/items/{itemId} を追加"
  - "PublicItemDetail 等の components schema を追加"

out_of_scope:
  - "Orval 再生成（1c）"
  - "internal-reco-api.yaml"
  - "apps 実装"

branch:
  name: "feature/task-416-pub-003-item-detail-openapi"
  base: "feature/epic-367-api-contract-orval"
  target: "feature/epic-367-api-contract-orval"

dependencies:
  epics:
    - issue: 367
      title: "[Epic]API-CONTRACT-ORVAL"
    - issue: 385
      title: "[Epic]API-PUB-003:商品詳細取得"
  issues:
    - "#399"
    - "#405"
  prs:
    - "#415"

parallel_control:
  exclusive_files:
    - "packages/contracts/openapi/public-api.yaml"

issue:
  unit: "task"
  type: "feature"
  area: "api"
