schema_version: "1.0"
definition_type: "contract"

contract:
  id: "contract-api-pub-008-feature-rule-masters-openapi-fragment"
  title: "API-PUB-008:Featureルール取得OpenAPI断片反映"
  summary: "API-PUB-008（GET /api/v1/masters/feature-rules）の契約仕様書に基づき public-api.yaml へ OpenAPI 断片を追加する。"
  type: "api_contract"
  priority: "high"
  status: "ready"

work_mode: "ai-agent"

source:
  discovered_from: "phase1_wave2"
  related_issue: "#404"
  related_pr: "#408"
  related_task_definition: "prompts/definitions/tasks/api-pub-008-feature-rule-masters/api-contract-spec.yaml"
  reason: "Phase1 1b 直列 merge（#367）。#427（PUB-007）完了後の次手。第2波 1b 最終 API。"

change:
  api_name: "Featureルール取得"
  api_kind: "public"
  endpoint: "GET /api/v1/masters/feature-rules"
  method: "GET"
  change_type: "add_endpoint"
  breaking_change: false

scope:
  - "public-api.yaml に GET /api/v1/masters/feature-rules を追加"
  - "BaseValueRuleMasterItem / ConceptFeatureRuleMasterItem 等の components schema を追加"

out_of_scope:
  - "Orval 再生成（1c）"
  - "internal-reco-api.yaml"
  - "apps 実装"

branch:
  name: "feature/task-428-pub-008-feature-rule-masters-openapi"
  base: "feature/epic-367-api-contract-orval"
  target: "feature/epic-367-api-contract-orval"

dependencies:
  epics:
    - issue: 367
      title: "[Epic]API-CONTRACT-ORVAL"
    - issue: 390
      title: "[Epic]API-PUB-008:Featureルール取得"
  issues:
    - "#404"
    - "#408"
  prs:
    - "#427"

parallel_control:
  exclusive_files:
    - "packages/contracts/openapi/public-api.yaml"

issue:
  unit: "task"
  type: "feature"
  area: "api"
