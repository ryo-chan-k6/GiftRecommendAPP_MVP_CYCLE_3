schema_version: "1.0"
definition_type: "task"

# テンプレート: フェーズ integration 再テスト Task（develop 直 PR）。

task:
  id: "task-mod-reco-001-execution-context-integration-retest-<phase-slug>"
  title: "MOD-RECO-001:ExecutionContext型付き化後 統合テスト再検証（<Phase Name>）"
  summary: "integration-test の getattr アサーションを ExecutionContext 型付き直接参照へ置換する。"

work_mode: "ai-agent"

parent:
  epic_branch: "develop"

background: |
  execution-context-typed-fields Task merge 後の follow-up Task（playbook 順序 4）。
  正本: prompts/definitions/tasks/mod-reco-001-recommendation-orchestrator/wiring-phase-follow-up-playbook.yaml

scope:
  - "test_recommendation_orchestrator.py の当該フェーズ getattr アサーション置換"

out_of_scope:
  - "execution_context.py 変更"
  - "stubs.py 変更"

branch:
  base: "develop"
  target: "develop"

dependencies:
  blocking: true
  tasks:
    - "task-mod-reco-001-execution-context-typed-fields-<phase-slug> が develop に merge 済み"

notes:
  - "作業開始ゲート: execution-context-typed-fields Task develop merge 後"
