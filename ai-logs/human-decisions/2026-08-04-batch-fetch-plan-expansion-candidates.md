# Human Decision Log

## 1. 概要

| 項目 | 内容 |
| ---- | ---- |
| Log ID | `2026-08-04-batch-fetch-plan-expansion-candidates` |
| Log種別 | `human-decision` |
| 件名 | BATCH-002/003 fetch_plan 拡大候補（追加ジャンルID・第1波・002/003優先・明示指定境界） |
| 発生日時 | 2026-08-04 |
| 記録日時 | 2026-08-04 |
| 発生元Command | `/work-issue @prompts/definitions/tasks/batch-fetch-plan-expand/expansion-candidates-decision.yaml` |
| 発生元Agent | `worker-ai` |
| workstream_key | `batch-fetch-plan-expand` |
| 関連Issue | #1844（本Decision） / #1843（親Epic） / #1827（ジャンル地図完了） / #1811（Phase1 cron・変更しない） / #1818（Phase2・変更しない） |
| 前提決定 | `2026-07-30-rakuten-fetch-ops-policy` / `2026-07-31-rakuten-fetch-mvp-fetch-plan` / `2026-08-03-batch-genre-map-campaign-ops-plan` |
| 重要度 | `high` |
| 状態 | **Human採択待ち**（AI推奨案提示済み。採択はHuman） |

本Logは、Epic #1843 の着手ゲートとして、ジャンル地図（`external_genre` 16,537）を根拠にした BATCH-002/003 の **追加ジャンル候補** を正本化する。  
拡大 live 収集・定常crontab変更・AI `--live-rakuten`・GHA楽天live・#1607・secret実値は含めない。  
**MVP 4ID（`100000` / `100003` / `100004` / `100005`）は置き換えない（追加のみ）。**  
BATCH-003 genre ルートは **明示指定 ID のみ**（子・孫の自動展開なし）。

---

## 2. 結論（現状）

| No | 論点 | 状態 | 内容 |
| --: | ---- | ---- | ---- |
| 1 | MVP 4ID | **維持（前提確定）** | `100000` / `100003` / `100004` / `100005` を置き換えない。追加のみ |
| 2 | 明示指定境界 | **維持（前提確定）** | BATCH-003 genre は fetch_plan / CLI で明示した ID のみ。地図の子・孫を自動展開しない |
| 3 | BATCH-002 vs 003 優先 | **AI推奨 → Human採択待ち** | **BATCH-003（商品Item）を優先**。BATCH-002 は第1波新IDの smoke 後に必要最小（`max_pages=1`） |
| 4 | 第1波追加ID | **AI推奨 → Human採択待ち** | 推奨案A: **`101381` / `551167` / `510901` / `216129`**（計4。§4） |
| 5 | 第1波件数・粒度 | **AI推奨 → Human採択待ち** | level1 の贈答隣接を明示指定。細分化（level2+）は第2波以降 |
| 6 | 定常cron反映 | **別Task・未着手** | #1811 / #1818 の crontab 変更は本Logでは行わない。採択後の別Task・Human承認 |

Humanが §4 推奨案を採択・修正・保留した時点で、本Logを `decided` に更新し、後続 fetch_plan反映 / 段階収集 Task の着手ゲートを開く。

---

## 3. 境界（変更しない）

| 項目 | 扱い |
| ---- | ---- |
| MVP fetch_plan 4ID | **置き換え禁止**。承認済み内容は [2026-07-31 Log](./2026-07-31-rakuten-fetch-mvp-fetch-plan.md) のまま |
| 子・孫自動展開 | **導入しない**。地図は選定の情報源であり、BATCH-003 の走査対象は明示IDのみ |
| keyword route | 本格常用化しない（必要なら別Decision） |
| 拡大 live 収集 | 本Logの採択後の **別Task**。本Logでは実行しない |
| 定常crontab（#1811 / #1818） | **変更・完了操作しない** |
| AI `--live-rakuten` | **禁止** |
| GHA楽天live / #1607 / `on.schedule` | 対象外維持 |
| secret実値 | docs / Issue / PR / 本Log に記載しない |

---

## 4. 追加ジャンルID候補と第1波推奨

### 4.1 選定観点（AI）

1. **贈答・ギフト隣接**（カタログギフト、スイーツ、酒、ジュエリー等）
2. **MVP 4ID と重複しない**（追加のみ）
3. **明示指定可能な単一ID**（BATCH-003 が自動展開しない前提）
4. **第1波は少数**（Run予算・cursor 増の定性影響を抑える）
5. **広すぎる level1 は第2波以降**（例: `100227` 食品は子が多く総量増が急）

### 4.2 候補プール（地図根拠・追加候補）

local `external_genre`（完了時点 16,537 / level0–5）から、MVP 以外で贈答隣接の **level1** を候補化する。  
名称・IDは地図読取結果（2026-08-04）。secretなし。

| 区分 | external_genre_id | genre_name | 第1波 | 備考 |
| ---- | ----------------: | ---------- | ----- | ---- |
| 推奨第1波 | **101381** | カタログギフト・チケット | **A** | ギフト直結 |
| 推奨第1波 | **551167** | スイーツ・お菓子 | **A** | 贈答定番 |
| 推奨第1波 | **510901** | 日本酒・焼酎 | **A** | 贈答定番 |
| 推奨第1波 | **216129** | ジュエリー・アクセサリー | **A** | 記念・フォーマル |
| 第1波任意加算 | 558944 | キッチン用品・食器・調理器具 | B | 結婚祝い等。件数影響は中 |
| 第1波任意加算 | 100939 | 美容・コスメ・香水 | B | 贈答多いが広い |
| 第1波任意加算 | 100001 | 百貨店 | B | MVP親 `100000` 配下の明示子（未採用ID） |
| 第2波以降 | 100227 | 食品 | C | 直下子が多く総量増が急。細分化後が望ましい |
| 第2波以降 | 510915 | ビール・洋酒 | C | 酒系の続き |
| 第2波以降 | 100804 | インテリア・寝具・収納 | C | |
| 第2波以降 | 216131 | バッグ・小物・ブランド雑貨 | C | |
| 第2波以降 | 566382 | おもちゃ | C | |
| 第2波以降 | 100533 | キッズ・ベビー・マタニティ | C | |
| 参考（MVP配下） | 100002 / 101736 / 101972 | 総合通販・その他・スーパー | — | `100000` 直下。ギフト特化は弱い |
| 参考（MVP配下） | 113084 | 花・観葉植物 | — | `100005` 配下。親が既にMVP。重複走査になりやすい |

**事実:** `100003`（贈答品・ギフト）・`100004`（輸入雑貨）は地図上で **直下子 0件**（leaf相当）。配下細分化IDは地図から取れないため、拡大は **別level1 / `100000` 兄弟** 側で行う。

### 4.3 第1波推奨案（案A）

| 項目 | 推奨 |
| ---- | ---- |
| 追加ID（明示） | **`101381` / `551167` / `510901` / `216129`** |
| 件数 | **4**（MVP 4ID は維持したまま加算） |
| 粒度 | level1。level2+ の明示指定は第2波 |
| BATCH-003 | 上記4を genre route に **追加**。既存4IDは継続 |
| BATCH-002 | 第1波では **後回し**。003 の1ジャンル smoke（低値）後、同じ新IDを `max_pages=1` で必要最小追加 |
| keyword | なし |
| 子・孫自動展開 | なし |

拡大後の承認済みスコープ（案A採択時のイメージ）:

```text
MVP維持: 100000 / 100003 / 100004 / 100005
第1波追加: 101381 / 551167 / 510901 / 216129
合計明示ID: 8
```

### 4.4 代替案

| 案 | 内容 | メリット | デメリット |
| --- | ---- | -------- | ---------- |
| **A（推奨）** | 第1波 4ID（§4.3） | 贈答隣接・件数抑制・判断しやすい | 食品・キッチン等は後回し |
| B | 案A + `558944` / `100939`（最大6） | 贈答カバーが広がる | cursor・API量が増えやすい |
| C | level1を広く（食品含む8+） | カタログ拡大が速い | Run予算・監視閾値抵触リスク大。第1波向きでない |
| D | 追加保留（MVP 4IDのみ継続） | 安全 | 地図成果を商品拡充に使えない |

---

## 5. BATCH-002 / 003 優先と Run予算影響（定性）

### 5.1 優先（AI推奨）

| Batch | 第1波での扱い |
| ----- | ------------- |
| **BATCH-003** | **優先**。商品Item経路でバリエーションを増やす（Epic方針と一致） |
| **BATCH-002** | 併走しない。003 smoke 後に新IDを `max_pages=1` で必要最小。`ranking_supplement` backlog がある場合は既存方針どおり 003 が優先消化 |

### 5.2 Run予算影響（定性・推論）

| 変化 | 影響 |
| ---- | ---- |
| genre cursor 数 | 明示IDが増えた分だけ active cursor が増える（深さ打ち切りなし方針は維持） |
| 1 Run のノブ | §5.3.4（`pages_per_run=60` 等）は **変更しない**。総量増は **Run回数・日数** で吸収 |
| 監視 | §5.3.5（429・API呼出/日・容量）を継続。超過時は Run頻度・対象一時縮小（深さ方針は切らない） |
| QPS / 同時live | 常用2・安全側1・楽天live同時1本は維持 |

---

## 6. Humanに決めてほしいこと

1. **第1波の追加ジャンルID一覧**（案Aの4IDを採択するか、B/C/Dまたは個別差し替えか）
2. **BATCH-003 優先でよいか**（BATCH-002 を第1波から併走させるか）
3. **第1波件数・粒度**（level1のままか、特定IDだけ level2 明示にするか）
4. **定常cron（#1811）への反映タイミング**（採択直後は別Task化のみか、反映時期の目安）

### 6.1 Human採択欄（記入用）

| 論点 | Human記入 |
| ---- | --------- |
| 判断者 | |
| 判断日時 | |
| 第1波追加ID（確定一覧） | |
| BATCH-002/003優先 | |
| 案（A/B/C/D/その他） | |
| 判断理由 | |
| 後続Task指示 | |
| 本Log状態 | `decided` / 保留 |

---

## 7. 判断後に必要な対応

| 対象 | 内容 | 着手条件 |
| ---- | ---- | -------- |
| 本Log | Human採択を §2 / §6.1 に反映し `decided` 化 | Human記入後 |
| 楽天Fetch運用方針 | §10 No.12 / §11.6 を採択結果と同期 | 本PRで推奨接続。`decided` 後に文言確定 |
| 後続 Task | fetch_plan 反映（設定/CLI伝播） | **本Log `decided` 後** |
| 後続 Task | 段階収集（local live・Human） | fetch_plan反映後。AIは `--live-rakuten` しない |
| #1811 / #1818 | crontab反映は **別Task・Human承認** | 本Logでは触らない |
| GHA / #1607 / schedule | 対象外維持 | — |

---

## 8. 確認した事実

- ジャンル地図キャンペーン完了により local `external_genre` は **16,537**（level0–5、queue空）。正本: [完了docs](../../docs/15_運用・改善/運用手順/ジャンル地図キャンペーン_live実行結果_完了.md)
- MVP 4ID はすべて present。Decision 2026-08-03 No.9 により地図は拡大情報源であり置き換えない
- level1 は **39** 件。MVPはうち `100000` / `100005`（level1）と `100003` / `100004`（level2）
- `100003` / `100004` の直下子は地図上 **0件**
- BATCH-003 は genre / keyword / update_sort / ranking_supplement の複数route。keywordは fetch_plan明示時のみ（既存）
- 本格収集（#1798）は MVP 4ID段階で一旦終了（案A）。本Logは **その先の拡大** のゲート
- #1811 / #1818 は本Task out of scope（Relationships追跡のみ）

---

## 9. 推論

- 第1波を贈答隣接の level1・4件に抑えると、cursor増と監視抵触リスクを抑えつつ地図成果を商品拡充に接続できる
- `100227`（食品）を第1波に入れると、継続走査の総量が急増しやすい（子が多い）
- BATCH-002 を第1波から併走すると、ranking API と item search の live が時間帯で競合しやすく、同時1本制約下では 003 進捗が遅れる

---

## 10. 関連情報

| 種別 | 参照 |
| ---- | ---- |
| 関連docs | [楽天Fetch運用方針](../../docs/15_運用・改善/運用手順/楽天Fetch運用方針.md) / [ジャンル地図完了](../../docs/15_運用・改善/運用手順/ジャンル地図キャンペーン_live実行結果_完了.md) / [棚卸し](../../docs/15_運用・改善/運用手順/ジャンル地図キャンペーン_external_genre棚卸し.md) / BATCH-002・003仕様書 |
| 関連Issue | #1844 / #1843 / #1827 / #1811 / #1818 / #1798 / #1745 |
| 関連Branch | `chore/task-1844-expansion-candidates-decision`（base: `chore/epic-1843-batch-fetch-plan-expand`） |
| Definition | `prompts/definitions/tasks/batch-fetch-plan-expand/expansion-candidates-decision.yaml` |
| 前提Decision | [MVP fetch_plan](./2026-07-31-rakuten-fetch-mvp-fetch-plan.md) / [ジャンル地図枠](./2026-08-03-batch-genre-map-campaign-ops-plan.md) / [運用値](./2026-07-30-rakuten-fetch-ops-policy.md) |
